# ROADMAP.md — Three.js Engine Phases

> Source of truth cho toàn bộ hệ thống module của THREEJS engine.
> Trạng thái realtime: `SYNC.md` (snapshot) + Living Index trong `CLAUDE.md` (auto-update).
> Project timeline theo tuần → `00-Threejs/ROADMAP.md`.

---

## Phase A — Environment Foundation _(✅ hoàn thành 2026-05-13)_

Mục tiêu: nền tảng shader + util cho mọi scene. Không có Phase A → không build được gì tiếp.

Exit criteria: tất cả module unit-pass + 00-Threejs import ít nhất 1 shader thành công.

| #   | Module            | Category | Status       | Dependency      |
| --- | ----------------- | -------- | ------------ | --------------- |
| 1   | `GlobalUniforms`  | utils    | ✅ unit-pass  | —               |
| 2   | `RuntimeGuard`    | utils    | ✅ unit-pass  | —               |
| 3   | `TriplanarMapping`| shaders  | ✅ unit-pass  | GlobalUniforms  |
| 4   | `WorldNoise`      | shaders  | ✅ unit-pass  | GlobalUniforms  |
| 5   | `RoundedCorners`  | shaders  | ✅ unit-pass  | —               |

---

## Phase B — Advanced Environment & Splats _(✅ hoàn thành 2026-05-13)_

Mục tiêu: LOD, procedural destruction, interior occlusion, particle system.

| #   | Module               | Category   | Status       | Dependency        |
| --- | -------------------- | ---------- | ------------ | ----------------- |
| 1   | `LODSystem`          | utils      | ✅ unit-pass | RuntimeGuard      |
| 2   | `ProceduralFracture` | shaders    | ✅ unit-pass | WorldNoise        |
| 3   | `InteriorMapping`    | shaders    | ✅ unit-pass | GlobalUniforms    |
| 4   | `GPUParticleSystem`  | components | ✅ unit-pass | —                 |
| 5   | `SparkSystem`        | components | ✅ unit-pass | GPUParticleSystem |

---

## Module Gallery _(✅ hoàn thành 2026-05-14)_

Mục tiêu: giao diện trực quan cho toàn bộ module library — live Three.js canvas mini cho từng module.

- `gallery.html` + `gallery.ts` + `gallery.css` — trang gallery standalone trong `00-Threejs/`
- `galleryCard.ts` — DOM card builder, IntersectionObserver lazy-load
- `galleryModules.ts` — danh sách 10 modules + dynamic import factory
- 10 `example.ts` refactored sang `export async function createDemo(canvas)` pattern
- `vite.config.js`: thêm `threejs-modules` alias + gallery build entry + fix ESLint checker
- Truy cập: `http://localhost:3000/gallery.html`

---

## Phase C — Character Pipeline _(chờ Phase B)_

Mục tiêu: VAT animation, billboard LOD, crowd pooling.

| #   | Module          | Category   | Status       | Dependency   |
| --- | --------------- | ---------- | ------------ | ------------ |
| 1   | `VATShader`     | shaders    | ✅ unit-pass | GlobalUniforms |
| 2   | `LODBillboard`  | components | ✅ unit-pass | LODSystem    |
| 3   | `CharacterPool` | utils      | ✅ unit-pass | RuntimeGuard |

---

## Phase D — Polish & Deploy _(✅ hoàn thành 2026-05-15)_

Mục tiêu: post-processing, animation, dynamic lighting. Đạt performance budget → deploy.

Exit criteria: < 100 draw calls, < 500k tris, < 16.6ms/frame → live demo Vercel.

| #   | Module           | Category   | Status       | Dependency     |
| --- | ---------------- | ---------- | ------------ | -------------- |
| 1   | `PostProcessing` | components | ✅ unit-pass | GlobalUniforms |
| 2   | `WindAnimation`  | shaders    | ✅ unit-pass | WorldNoise     |
| 3   | `DayNightCycle`  | utils      | ✅ unit-pass | GlobalUniforms |

---

## Phase E — Interaction & Animation _(✅ hoàn thành 2026-05-18)_

Mục tiêu: user tương tác được với object 3D, character chạy animation từ glTF, scene phản ứng theo scroll.

Exit criteria: click vào mesh trigger event, glTF animation play/crossfade, scroll điều khiển camera path.

| #   | Module              | Category | Status       | Dependency     |
| --- | ------------------- | -------- | ------------ | -------------- |
| 1   | `InteractionSystem` | utils    | ✅ unit-pass | RuntimeGuard   |
| 2   | `AnimationSystem`   | utils    | ✅ unit-pass | RuntimeGuard   |
| 3   | `ScrollTimeline`    | utils    | ✅ unit-pass | GlobalUniforms |

---

## Phase F — Physics _(✅ hoàn thành 2026-05-18)_

Mục tiêu: object rơi, va chạm, character di chuyển có physics. Dùng Rapier.js (WebAssembly).

Exit criteria: rigid body rơi đúng gravity, CharacterController di chuyển + jump, debug visualizer hoạt động.

| #   | Module                | Category | Status       | Dependency   |
| --- | --------------------- | -------- | ------------ | ------------ |
| 1   | `PhysicsWorld`        | utils    | ✅ unit-pass | RuntimeGuard |
| 2   | `RigidBody`           | utils    | ✅ unit-pass | PhysicsWorld |
| 3   | `CharacterController` | utils    | ✅ unit-pass | PhysicsWorld |
| 4   | `CollisionEventBus`   | utils    | ✅ unit-pass | PhysicsWorld + RigidBody |

---

## Phase G — Audio _(✅ hoàn thành 2026-05-23)_

Mục tiêu: spatial audio system — positional SFX tại world position, tích hợp CollisionEventBus choreography.

| #   | Module        | Category | Status       | Dependency   |
| --- | ------------- | -------- | ------------ | ------------ |
| 1   | `AudioSystem` | utils    | ✅ unit-pass | —            |

---

## Phase H — Procedural World _(⏳ kế hoạch)_

Mục tiêu: tạo nội dung procedural — terrain, building, prop — không cần asset file.

| #   | Module               | Category | Status  | Dependency          |
| --- | -------------------- | -------- | ------- | ------------------- |
| 1   | `TerrainSystem`      | utils    | ⏳ plan | WorldNoise          |
| 2   | `ProceduralBuilding` | utils    | ⏳ plan | TerrainSystem       |
| 3   | `CityLayout`         | utils    | ⏳ plan | ProceduralBuilding  |

---

## Phase I — Scene Streaming _(⏳ kế hoạch)_

Mục tiêu: load/unload chunks theo camera position — city lớn không giới hạn bởi GPU budget.

| #   | Module          | Category | Status  | Dependency      |
| --- | --------------- | -------- | ------- | --------------- |
| 1   | `ChunkManager`  | utils    | ⏳ plan | CityLayout      |
| 2   | `AssetStreamer` | utils    | ⏳ plan | ChunkManager    |

---

## Phase J — Navigation _(⏳ kế hoạch)_

Mục tiêu: NPC tìm đường trong city — NavMesh bake + pathfinding.

| #   | Module           | Category | Status  | Dependency  |
| --- | ---------------- | -------- | ------- | ----------- |
| 1   | `NavMesh`        | utils    | ⏳ plan | CityLayout  |
| 2   | `NPCController`  | utils    | ⏳ plan | NavMesh     |

---

## Changelog

| 2026-06-10 | **🪨 Đá viền hồ KHÉP KÍN — consumer mới của kệ ops (op #1 arcLength).** Nâng cấp "chờ Factory commit ops/" mở khóa sáng nay. `pondStoneGeos` bỏ walk tay segment-by-segment: polyline ring → curve fn khép kín + `arcLength` (ops/resample); sinh hết bước adaptive (r_i+r_{i+1})×0.82 trước rồi **chia lại khít chu vi (k = L/Σstep)** → vòng đá KHÉP KÍN không mối nối (walk cũ dừng đâu hở đó — seam last→first tùy chu vi); bước cuối khớp viên 0 (neighbor wrap). **Điều chỉnh sổ cũ: `copyToPoints` KHÔNG dùng cho viền** — mỗi viên jag geometry riêng đã merge 1 draw, instancing ép share geometry (mất jag per-viên) mà 0 lợi draw call. Gates tsc/eslint/check-perf xanh. |
| 2026-06-10 | **🌍 Vòng MÔI TRƯỜNG — plan chốt, ghi sổ deferred (chưa build).** NgQuan: "Gaea sẽ là chỗ CHÍNH dựa vào phát triển — cần kết nối gần nhất có thể" → research + verify license Gaea 2 chính chủ: **CLI automation (`Gaea.Build.exe` + variable override) chỉ Professional $199**; Community 0đ = 1K/non-commercial; Gaea 3.0 preview 12/2025 (đừng mua 2 Pro vội). Kiến trúc chốt: **kết nối = file contract `.r32`** (float32 trần → `fetch→Float32Array`, zero dependency) chung mọi tier — Tầng 0 Community: Gaea Build → watcher Factory đẩy `assets/environments/` → browser tự cập nhật; Tầng 2 Pro: + daemon slider-điều-khiển-Gaea. Phases E0 (TerrainRing module, heightData API từ ngày 1, FBM fallback) → E0.5 (env field site-kit + tab 🌍) → E1 (cầu Gaea watcher) → E2 (vách đá bake + thác Phase B) → E3 (polish). Cấu trúc thư mục đã phân vai (lõi component / site-kit state / vỏ gui/env.ts / Factory gaea/ / assets environments/); CHƯA tạo domain env/ (luật ≥3 cụm). Toàn bộ tại `deferred/systems/terrain-gaea-heightmap.md` 📐 PLANNED — chờ lệnh, bước 0 = cài Gaea 2 Community. |
| 2026-06-10 | **◯ Round follow-up — Walls section GỌN (archplan): hết N tab tràn panel.** NgQuan: "quá nhiều bức tường thon tạo hình trụ khiến tab đầy màn hình" → round KHÔNG trải 6–24 tab nữa: `buildWallsCompact` = dropdown **Mặt #** + 1 folder của mặt đang chọn + nút **"Áp mặt này → TẤT CẢ mặt"** (`applyLookAllFacets` — chép wallH/material/tham số look, KHÔNG chép openings/panels kẻo cửa nhân bản N mặt; field list mirror copySegExtras). Mặt chọn nhớ qua `_activeTabs` (key `wallSel:instId` — sống qua rebuild, tự clamp khi Sides giảm). **Hệ Focus thêm biến thể `registerFocusAction`** (manipulate.ts + ctx): section gọn chỉ có 1 folder → mặt KHÔNG-chọn đăng ký ACTION thay folder; action chạy TRƯỚC tra anchor (đổi chọn + rebuild GUI → anchor mới tự đăng ký) — **3D click mặt nào picker nhảy mặt đó**, mở + flash đúng folder. Eslint ≤50 dòng ép tách `_focusCtx` (pattern `_floorCtx`). INTERACTIONS.md +mục "Section GỌN". Shapes vuông (rect/T/L/U ≤8 tường) giữ tab như cũ. |
| 2026-06-10 | **◯ Shape ROUND — tháp/khối trụ tròn N-gon (building-kit + archplan).** NgQuan: "quay lại tường — phase tường tròn và shape hình trụ tròn" (phase chưa từng ghi sổ — lập mới). Soi lõi: `turtle.ts` chịu `turnBefore` TÙY Ý sẵn (giới hạn ±90° chỉ ở GUI dropdown + presets) → preset `round` trong SHAPE_CONFIGS (radius 1–10m + sides 6–24, chord=2r·sin(π/N), turn=360/N — industry-style arc tessellation như Revit/SketchUp): mỗi facet = 1 tường THƯỜNG, opening/material/wallH/brick-3d per-facet dùng nguyên hệ, Pick/Move/Focus/Shape-Group khỏi wire thêm. **Slab + móng concrete theo ĐÚNG đa giác** thay AABB chữ nhật (4 góc hết thò): `planOutline` (turtle, đỉnh centered local) → `instOutlineLocal` (build.ts) → `outline?` opts qua `outerBoundary` trong makeSlabWithHoles (lỗ khoét giữ nguyên); móng nở đều wallDepth/2 + overhang TB 4 hướng (`expandOutline` — N-gon đều: offset cạnh ≈ scale bán kính); wood-deck/stone-pillar giữ rect/radial (stone-pillar toả tâm hợp tròn sẵn). GUI vỏ: dropdown ◯ + slider Sides đổi SỐ tường → `_onDimChange` tự `_rebuildGUI` khi count đổi (live drag giữ GUI — không destroy controller đang kéo). 0 schema bump (preset thuần — dims là Record sẵn). Gates: tsc/eslint/check-perf xanh, vite transform 3 tầng OK. **NỢ:** mái trên round vẫn theo bbox (gable/hip sai dáng — tạm dùng flat/parapet) → **mái NÓN = đất Factory**; instWorldAABB/nested-carve vẫn vuông (round w=d nên swap vô hại, lỗ carve vuông dưới slab tròn chấp nhận v1). |
| 2026-06-10 | **🪨 Đá viền hồ DẸT-BÈ + trả nợ eslint fence.** NgQuan: "dẹt ngang, hạ thấp độ random chiều cao, bè bè bớt nguy hiểm" → `stoneAt`: ngang 0.95..1.35 (bè), cao **0.42..0.56** (dẹt, range hẹp = lùn đều), tâm 0.4r→0.3r; scale Y áp SAU jag → góc cạnh cũng bẹp. Kèm ("tiện sửa luôn" — NgQuan duyệt): **trả nợ eslint 3d8d82f** — `stoneWallEdge` 74-dòng/cx-13 → tách `stoneWallVerts`+`stoneWallIndices`; `buildSiteFence` cx-19 → tách `fenceGeosFor`+`fenceMaterialFor`. Code DI NGUYÊN VĂN, 0 đổi hành vi; fromState.ts sạch eslint TOÀN FILE. |
| 2026-06-10 | **🪨 Đá viền hồ — to-nhỏ xen kẽ + góc cạnh (site-kit + GUI).** NgQuan: "viền đá to hơi đều… thêm random to nhỏ xen kẽ + chút góc cạnh (min max random từ tâm ra các cạnh), đừng tròn quá". State `WaterConfig` +`borderStoneVar`/`borderStoneJag` (% 0–100, default 45/35 — áp cả save cũ theo chủ ý, parse tolerant không bump schema). Render `pondStoneGeos`: bán kính per-viên ×[1±0.45v] + **bước xếp tự giãn (r_i+r_{i+1})×0.82** (viên to chiếm chỗ rộng, vẫn xếp liền); `jagVertices` — đỉnh icosa lệch bán kính [1±0.45j] từ tâm, sin-hash theo (VỊ TRÍ đỉnh, idx viên) → vertex trùng vị-trí lệch GIỐNG nhau (icosa non-indexed không nứt mặt — bài KI-003), `computeVertexNormals` giữ faceted. GUI: +2 slider "Đá to-nhỏ %"/"Đá góc cạnh %" (chỉ shape cong, commit-only né reflector thrash). Ghi chú: câu hỏi gốc "dùng ops/ bo cạnh hồ?" → trả lời KHÔNG đáng (bezier handles đã giải, mạnh hơn bevelProfile); chỗ ops đáng giá quanh hồ = `resampleCurve`+`copyToPoints` cho viền đá ĐỀU-theo-chiều-dài-cong (nâng cấp tương lai, chờ Factory commit ops/). ⚠️ Lộ 2 lỗi eslint PRE-EXISTING `stoneWallEdge`/`buildSiteFence` (3d8d82f 06-05) — ngoài vùng, chưa xử. |
| 2026-06-10 | **🧲 Shape Group P1+P2a — chọn nhóm khối + ghost-drag cả nhóm (archplan, WANTED 2026-06-05).** Gác thác (quay lại vòng MÔI TRƯỜNG với Gaea — memory `project-thac-nuoc-paused-for-gaea-env`), quay về hàng chờ editor. NgQuan duyệt plan ĐÃ DE-RISK: tách "kéo nhóm" khỏi "kéo nhóm LIVE" — **P2a ghost-drag KHÔNG đụng split-render KI-009** (chỗ từng tốn nhiều công fix fps nhất), P2b live-translate multi = hoãn có chủ đích (chỉ làm nếu ghost chưa đủ). **Kiến trúc chốt:** ad-hoc `Set<instId>` ở VỎ (zero schema — không bump DESIGN_SCHEMA_V, né KI rename-persist) · v1 cùng tầng · snap nhóm = bbox UNION. **Build:** (1) `interaction/selection.ts` MỚI — `ShapeSelection`: viền cyan ỔN ĐỊNH per-khối (khác flash vàng highlight; box từ `instAABB` lõi + instWallBase) + GHOST nhóm (viền gốc đứng yên — thấy cả nguồn lẫn đích) + refresh sau mọi `_buildScene` (viền bám pos mới, khối bị xoá tự rớt). (2) `manipulate.ts` — DragSession 'inst' +group/gdx/gdz; `_tryGroupGhost` (kéo khối thuộc nhóm ≥2 → ghost, KHÔNG split; kéo ELEMENT trong khối nhóm vẫn đường thường); `_ghostMove` + Ctrl-snap `_groupSnap` (union dịch theo Δ vs khối NGOÀI nhóm); `_commitGroup` = **CÙNG 1 Δ-đã-round cho mọi khối** (round riêng = trôi tương đối 1mm) + rebuild 1 lần; `pickInstId` public cho Shift+click. (3) `snap.ts` +`unionAABB`/`shiftAABB`. (4) Lab wiring: Shift+click toggle (`_shiftToggleSelect` — nuốt event kể cả trượt) · Esc xả (`_escClearSel`; click-trống KHÔNG xả — né nhầm) · rời Move mode tự xả · dispose. **Eslint complexity ≤10 ép tách 6 guard-helper** đúng house-style (`_isTypingTarget`/`_escClearSel`/`_shiftToggleSelect`/`_initShapeSel`/`_disposeOverlays`/`_ghostMove`). INTERACTIONS.md +block 🧲. Deferred shape-group-system → ✅ P1+P2a, P2b/P3 chờ nhu cầu thật. |
| 2026-06-10 | **🌊 Thác A4+A5 — nâng độ "thật" theo KHẢO SÁT INDUSTRY (Waterfall 1.3→1.6.0).** NgQuan: "nước chưa thật lắm, dạo 1 vòng các phần mềm" → khảo sát Season/RealtimeVFX-thread/Jay-Broderick/Cyanilux/GDC-Uncharted/LiquiGen-1.0 → chốt 4 gap: texture sọc-kẻ-synthetic · màn phẳng lì · thiếu lớp (foam chân/highlights) · unlit. **A4 (đã trình trade-off perf, NgQuan duyệt):** (1) `makeStreakTexture` viết lại = **VORONOI SỢI** lattice CHU KỲ 12×4 (cell ~128px = sợi dài, tự tileable — bỏ trick vẽ-lặp) + value-noise 2-octave distort (hệ số NGUYÊN giữ chu kỳ) + độ đậm per-sợi theo cell-id; hash số nguyên thuần KHÔNG Math.random (tái lập). Giá: ~30–100ms JS per-pixel BUILD-TIME mỗi structural commit, runtime 0 đổi. (2) **WPO bulge** — phồng 3D: sin 2-octave THUẦN MATH trong vertex (không sample texture — bẫy textureSampleLevel) cuộn xuống, neo lip, biên ăn theo surge; lưới 16×40→**24×60** (2880 tri); slider `Phồng 3D`; normal KHÔNG tính lại (stylized chấp nhận). **A5:** (3) **VÒNG BỌT LOANG chân** kiểu Season bottom-foam — plane ngang 2-tri từ đường chạm loang ra, texture ĐỐM TRÒN radial-gradient 9-vị-trí tileable (vài ms, deterministic), 2 lớp lệch tốc NHÂN nhau (blob tan/hợp), gate flow-strip (khe cắt dòng → bớt bọt) × surge, depthWrite=false + renderOrder 2 + nâng 12mm (sẵn nằm trên mặt hồ Phase B); slider `Bọt chân`. (4) **LẤP LÁNH** — glint OVERBRIGHT lõi sợi sáng nhất (smoothstep 0.82–0.97) × fresnel × surge-đảo (êm/láng rõ — đục chìm), GỘP vào shader màn+crest (phương án 0-draw 0-overdraw đã chọn ở bảng trade-off); slider `Lấp lánh`. Tổng: **5 draw · ~3.5k tri · 0 RTT · mọi thứ mới live**. Gate đủ. Hero-tier ghi sổ: depth-fade (+1 copy depth/frame — đắt) + LiquiGen/Houdini flipbook (VRAM MB-chục-MB, đường Factory). |
| 2026-06-10 | **🌊 Thác A3.x — HỆ ĐIỀU KHIỂN 3 TẦNG: điều dòng ngang × theo đoạn rơi × master Nguồn + CREST mặt ngang (Waterfall 1.1→1.3).** NgQuan: "điều khiển dòng tốt hơn — chia mảng nhỏ, cuộn xiết, tách dòng" → kiến trúc **strip-control LIVE 0-rebuild 0-recompile** (ghi 256–512 byte DataTexture; unorm8 KHÔNG FloatType — `float32-filterable` là optional feature). **(A3) Strip NGANG 64×1** sample `uv.x` (R=tốc-cột/2 · G=mật-độ/CẮT-DÒNG · B=xiết) — `setFlowProfile(samples)` nội suy linear; xiết = méo-UV + foam cục bộ FRAGMENT-ONLY (không đổi hình học — không sample texture ở vertex); Lab: 4 slider Số-dòng/Hở-khe/Xiết/Lệch-tốc → `flowPattern` sinh profile (xiết đậm ở RANH dòng). **(A3.1) Strip DỌC 64×2** sample `1−uv.y`, 2 hàng texel (tâm y=0.25/0.75 — linear không lẫn hàng): trong-suốt/gương(sheen-fresnel)/bụi-tán/nhiễu/vỡ-hạt(alpha-erosion theo vệt = mép răng cưa) — `setFallProfile`; Lab: chip Đỉnh/Giữa/Chân + 5 slider re-render theo đoạn. **(A3.2) `surge` MASTER vật lý** (NgQuan: "nhẹ → đầu thác trong veo không gợn; mạnh → phá cấu trúc bọt trắng xóa") — 1 uniform đan **7 điểm** shader, MỌI hệ số `mix()` qua TRUNG TÍNH ×1 tại 0.5 (không phá look đã tune): glass-zone kéo-sâu↔tắt · aeration pow/weight · vệt ×0.4↔1.6 · méo ×0.5↔1.5 · wobble ×0.2↔1.8 · mist ×0.2↔1.8 · alpha ×0.7↔1.3 · surge>0.65 tự VỠ HẠT nền; né chia-0: edge0 smoothstep không cho chạm edge1. **(A3.3) CREST** — mặt nước NẰM NGANG trên đỉnh trước khi đổ (NgQuan yêu cầu): plane XZ mép tại z=0 khớp lip, **DRAWDOWN vật lý** (upstream cao = `crestDepth` võng smoothstep về y=0 ĐÚNG MÉP — nối liền không hở); DÙNG CHUNG flow-strip (tách dòng nối tiếp mặt-ngang→màn); KHÔNG khúc xạ (alpha thật — né +1 copy framebuffer KI-014); 2 structural (crestLength/Depth — tường preview tự nới sâu) + 3 live (Tốc/Gợn/Trong mặt); eslint max-lines → tách `structuralRows`/`crestRows`. Tổng Lab ~30 slider. |
| 2026-06-10 | **🌊 Thác A2.1 — chuỗi fix runtime WebGPU (KI-013 + KI-014) tới SẠCH console, verify OK.** Lab tab 🌊 lần đầu chạy lộ 3 tầng bug (debug bằng `2 ảnh console + ĐỌC SOURCE three, không đoán): **(1) KI-013 — THREE.Points trên WebGPU**: point LUÔN 1px + `pointUV` WebGL-only (emit GLSL thô) + `PointsNodeMaterial.setupVertex` lấy `positionGeometry.xy`×size làm offset clip-space → spawn-pos ≠ 0 = ~300 hạt văng trây = **"màn xanh phủ kín viewport"**. Fix: mist/splash → **InstancedMesh quad + SpriteNodeMaterial** billboard. **(2) KI-014 — `viewportSharedTexture` = `let _sharedFramebuffer` MODULE-GLOBAL**: editor (2044×1408) + Lab (1412×787) là 2 renderer cùng page giành 1 texture → flood `copy range touches outside` + command buffer invalid + dispose nổ. Fix: **`viewportTexture` per-instance** + giữ ref `.value` tự dispose (né leak VRAM cỡ canvas/rebuild). Cùng họ KI-012 — three giấu state module-global. **(3) Dispose attr instanced node-backed nổ `undefined.destroy` mỗi rebuild** (`destroyAttribute` `data.buffer.destroy()` không guard; gắn attr vào geometry KHÔNG cứu) → **redesign: bỏ HẲN custom attribute — spawn/hướng/phase sinh trong shader từ `instanceIndex + hash`** (zero attr = zero bề mặt lỗi; trade-off: phân bố chỉ đều, mất điều khiển per-hạt từ CPU). Kèm: slider Khung Lab → **commit-khi-buông** (`liveDrag=false` — kéo-rebuild 60/s = bão dispose-in-flight, vi phạm PERFORMANCE.md) + **`safeDispose` từng-bước-có-nhãn** trong module (1 bước nổ không nuốt các bước sau = hết leak; console tự khai tên bước — chính nó tóm `sprayGeo`). Trade-off đầy đủ đã trình NgQuan (quad ×4 vertex/hạt ~0 · copy framebuffer PER-THÁC phình theo N thác → Phase B cân shared-flag · mất live-drag khung → polish scale-giả sau). **Verify NgQuan: console sạch, thác chạy.** |
| 2026-06-10 | **📦 Assets — gom 23 texture → 5 HỌ `ground/ wall/ stone/ wood/ roof/` (roof chừa Factory).** NgQuan: "phân loại theo path… tạo thêm thư mục cho mái luôn". Đúng điều khoản PROTOCOL sẵn có (*gom theo HỌ khi ≥15-20 texture* — đã 23). **Assets repo:** 190 rename qua `git mv` (giữ history) + `roof/.gitkeep` + PROTOCOL.md cập nhật cấu trúc + REORG note + bảng map cho session khác. **Trỏ path:** archplan `ArchPlanLab.ts` 101 import + comment; `ground-lab.ts` (file Factory đang sửa dở) 9 import → **sửa path-only dòng 16–24** sau khi app chết vì import-analysis (lỗi resolve = chặn cả module graph, không phải overlay; đã nhắn Factory qua PROTOCOL note). REGISTRY.json tự update path mới khi validate. **Gặp + xử:** folder bị khóa rename — thủ phạm gồm dev server 01-Doraemon **bỏ quên từ 31/05** (kill) + watcher giữ dir-handle → chiêu **move-children** (git mv từng con + rmdir vỏ). **Nợ cũ lộ ra (CHƯA xử):** tên underscore (`rock_rough`, `coal_stone`…) fail luật kebab-case validate.js — sửa = đổi tên + trỏ path 1 đợt riêng sau khi Factory rảnh; `Wooden_Plank` vs `wooden_planks` nghi trùng chất liệu — rà giữ 1. |
| 2026-06-10 | **🌊 Thác nước Phase A2 — texture vệt + 3 lớp + fresnel + khúc xạ + splash; tab Lab 🌊 Thác.** NgQuan chê A1 "chưa đạt" + đưa công thức industry (sheet + texture cuộn nhiều lớp + fresnel + méo) + chốt QUY TRÌNH MỚI: *"khái niệm mới cần preview → KHÔNG tạo html rời, vào thẳng Lab tạo tab"*. **Module (`Waterfall` 1.0→1.1):** (1) **`makeStreakTexture`** — texture VỆT NƯỚC CanvasTexture 256×512 sinh 1 LẦN build-time (~150 vệt dọc fade 2 đầu, vẽ lặp ±w/±h qua translate → tileable 2 trục; gradient là user-space nên PHẢI translate — vẽ thẳng offset = mảng trong suốt) — thay triNoise blob (procedural không ra VỆT XỐI DÀI — đúng bài học "texture nhanh đẹp hơn"). (2) **3 lớp cuộn** A ×1 · B ×0.72 đảo-U · C ×1.5 (sample trước làm UV-distort A/B). (3) **Fresnel** màn cong (`computeVertexNormals` SAU uốn; `dot(eye,normalWorld).abs()` — DoubleSide). (4) **KHÚC XẠ màn** `viewportSharedTexture(screenUV+off·uRefract)` (+0 pass, cùng kỹ thuật WaterSurface) — màn gần đặc (alpha 0.95, depthWrite=true), "độ trong" từ ảnh backbuffer méo theo vệt. (5) **lip/foot band + glass-zone + posterize** (banding 4 nấc look RiME, uniform 0..1) + **vertex wobble** mép dưới (positionNode sin). (6) **Mist + SPLASH** 2 hệ chung 1 builder (`SprayCfg`) — sprite TRÒN MỀM `pointUV` radial fade (hết chấm vuông), NormalBlending. Gotcha né sẵn: smoothstep ĐẢO ngưỡng = undefined WGSL → `1−smoothstep(lo,hi,x)`. 13 props / 10 setter live uniform-cheap; 1280 tri + 2 hệ hạt = **3 draw, 0 RTT**. **Lab tab 🌊 Thác (archplan):** `gui/waterfall-preview.ts` (WaterfallPreview — mini-scene WebGPU riêng: tường+bể+thác, OrbitControls, ResizeObserver; structural width/height/arc/mist = rebuild throttle rAF; `tune(key,fn)` Map ghi-đè — KHÔNG tích lũy closure khi kéo slider, áp lại sau rebuild) + `gui/waterfall-lab.ts` (slider structural+live, doc công thức, ⚙ preset màu + nền sáng/tối) + đăng ký `EXPERIMENTS[]` lab-experiments.ts (cạnh Mái/Particles/Nền — KHÔNG đụng file Factory). XÓA preview html tạm (`src/waterfall-preview.*` — vite root=src từng làm 404). Gate: validate PASS + module tsc/eslint sạch + archplan eslint (file mình) sạch + check-perf 0 Waterfall-warn; **vite build đang vướng `ground-lab.ts` (Factory sửa dở song song — lỗi đổi liên tục, KHÔNG phải file mình)** → build xanh khi Factory chốt. **Chờ verify Lab tab 🌊** (dev :3002 → 🧪 Lab → 🌊 Thác). |
| 2026-06-10 | **🌊 Thác nước Phase A — module `components/Waterfall` (màn nước + mist, 0 RTT).** NgQuan duyệt kế hoạch sau rà-soát water: "làm phase trước đi… dựng tường trước để thác nước chảy xuống thử". **Kiến trúc (industry-standard sheet waterfall — KHÔNG fluid sim):** (1) **Màn nước** = ribbon `PlaneGeometry` 8×36 uốn **z = arc·√t** (nước rời mép NGANG rồi rơi: dạt ∝ √quãng-rơi — parabol vật lý, vọt ở mép → thẳng dần), gốc group = TÂM MÉP TRÊN (lip) → caller đặt vào mép đổ; `MeshBasicNodeMaterial` transparent DoubleSide depthWrite=false — **sọc foam = 2 octave `triNoise3D` kéo DÀI dọc cuộn XUỐNG** (cộng t vào toạ-độ-y noise; tần lệch ×2.3/×2.2 + nhanh 1.35× phá lặp — đúng kỹ thuật FBM sóng WaterSurface, đổi hướng cuộn), càng xuống càng TRẮNG (aeration=1−uv.y), sát lip = lưỡi nước trong (glass), fade mép trái/phải + 5% chân. (2) **Mist** = Points loop-lifetime (fract + phase offset — pattern GPUParticleSystem TỰ CHỨA, module không import chéo) spawn dọc đường-chạm, bốc lên + toả; NormalBlending (đọc đúng trên nền sáng lẫn tối). **10 props** (width/height/arc/flow/streakScale/waterColor/foamColor/opacity/mistCount/mistIntensity) + 7 setter live = **uniform-cheap, 0 rebuild/recompile** (hợp đồng PERFORMANCE.md) + getTriangleCount. **Budget: 576 tri + ≤600 hạt = 2 draw, 0 RTT, 0 texture** (rẻ hơn 1 hồ nước rất nhiều — không dính chuỗi bẫy reflector KI-006/007/008/012). `example.ts` = đúng kịch bản test: TƯỜNG đá + thác chảy xuống bể. **Vì sao không reflector:** màn đứng sủi trắng — mắt không đọc phản chiếu; reflector chỉ đáng cho mặt nước ngang. Gate: `validate.js` PASS + tsc + eslint 0-lỗi + check-perf 0-error (2 warn false-positive đã `// perf-ok`). **Phase B** = ráp archplan đổ vào pond (renderOrder sau mặt hồ) + GUI tab Water; **vách đá đỡ thác = Houdini bake** (`deferred/systems/houdini-bake-accents.md` — tường box là placeholder test). **Chờ verify preview** (example.ts xoay-ngắm). |
| 2026-06-10 | **🪨🗑️ Non bộ procedural — XÓA khỏi code (NgQuan duyệt: "không khả thi thì xóa đi").** Đánh giá: procedural cầu-displace KHÔNG cứu được (giới hạn PARADIGM — không ra vách dựng/đá-dẹt-xếp-tầng/hang, xác nhận bằng mắt 2026-06-08) + đường thay thế đã chốt = **Houdini bake** (`houdini-bake-accents.md` ⭐#1) → giữ code chết chỉ thêm nhiễu, git history giữ bản khôi phục. **Gỡ:** module `components/RockCluster/` (4 file) + catalog row; state (`RockConfig`/`SiteState.rocks`/`renderRocks`/`makeRock`/`parseRock(s)` — key `rocks` trong design lưu cũ được parseSite BỎ QUA an toàn, không bump schema); render (`buildRocks`/`SiteHandle.rocks`); GUI archplan (tab Rock + `buildRockDomain/Tabs/Pane`/`rockSliderSpecs`/`rockPosRow`; **`ROCK_MAT_OPTS` → `STONE_MAT_OPTS`** vì path-zone đang dùng chung; `pathSliderSpecs` tách type `PathSlider` riêng — trước mượn `RockSlider`); Lab (`_siteRocks`/`_tuneRock`/`_applyRocksLive`/`_rebuildRocksLive` + ctx `tuneRock`/`applyRocksLive`; `_usedBorderTexKeys` bỏ loop rock — cache đá triplanar GIỮ cho border hồ + path-zone); playbook `non-bo.md` xóa (lesson sống ở `deferred/non-bo-rockery-builder.md`, status cập nhật REMOVED). Gate: modules tsc + archplan tsc + eslint sạch (3 fence cố hữu) + check-playbooks PASS (index --write). |
| 2026-06-09 | **🪨🏔️ Path-zone — BÁM GÒ (đá theo cao-độ terrain).** NgQuan: "thêm bám gò cho path". **Reuse `layer.drape`** (KHÔNG thêm state/module): drape zone vốn NGOÀI `zoneRects` → terrain GIỮ gò nhấp nhô dưới nó (cơ chế surface-drape sẵn có). **Render (`fromState.ts`):** `addStonePathMesh` +param `drape: DrapeCtx`; khi `layer.drape && drape` → **`drapeStonesToTerrain`** dời Y MỖI viên `m.elements[13] += heightAt(hf, worldX, worldZ)` (worldXZ = offset + xoay local theo rot — Three Y-rot `x'=x·c+z·s, z'=−x·s+z·c`; mesh.rotation.y giữ Y nên world Y dời đúng; getMatrixAt/setMatrixAt + instanceMatrix.needsUpdate). **Cỏ vẫn né khuôn:** `siteGrassExclude` +rect MỌI path-zone (`layerRect`) — vì bám-gò (drape) NGOÀI zoneRects nên grass-exclude phải thêm tay; `layerRect`(MaskRect) structural-khớp `GrassExcludeRect`; KHÔNG ăn vào mask terrain (buildHeightField dùng zoneRects, không dùng siteGrassExclude) → gò giữ nguyên. Side-effect tốt: path-flat-không-terrain giờ cũng re-scatter cỏ khi đổi khuôn (siteGrassExclude ở trong grassBuildSig). **GUI (`gui/site.ts`):** toggle **Bám gò** trong `buildPathZoneBody` → `layer.drape` + applySite. **Honest:** bám-gò + xoay LIVE (`tunePathRotLive` chỉ quay, không re-sample heightAt) → đá lệch cao-độ vài cm lúc kéo Rotate°, buông re-drape đúng (gò thoải, chấp nhận); per-stone height = post-process matrix (rẻ, build-time). Playbook `lat-da.md` §2/§4/§5. Gate: module tsc + archplan tsc + eslint sạch + check-perf 0-error + Vite 7.38s. **Chờ verify :3002** (terrain bật + path → bật Bám gò → đá uốn theo gò; tắt → phẳng pad). |
| 2026-06-09 | **🪨⚡ Path-zone — FIX TỤT FPS khi xoay (+ mọi slider structural path).** NgQuan: "cơ chế xoay làm tụt fps". **Root cause:** slider Rotate (và R/gap/seed…) route `(v,c)=>(set(v), ctx.applySite(c))` → kéo (c=false) = `applySite(false)` → `_renderSite` (siteSig đổi) → `_rebuildSite` **tái-tạo water reflector RTT + recompile NodeMaterial MỖI frame** = bẫy fps kinh điển (PERFORMANCE.md / feedback-perf-fps-contract). Xoay chỉ là transform mà phải dựng lại cả nước. **Fix (archplan, KHÔNG đụng module/state):** (1) **Rotate° = live transform** — tách khỏi `pathSliderSpecs` thành `pathRotRow`; kéo → `ctx.tunePathRotLive(flatIdx, rotDeg)` → Lab `_tunePathRotLive` (`_layerMeshByIdx` tìm mesh theo groundLayerIdx → set `mesh.rotation.y` THUẦN, 0 rebuild). (2) **Slider structural path (R/gap/thick/seed/ellipse) + Color = `applyZonesLive`** — kéo → `ctx.applyZonesLive()` → Lab `_applyZonesLive` (rAF throttle → `_rebuildGroundLayersLive` = rebuild CHỈ zone meshes surface+path, KHÔNG đụng nước/cỏ/nền). Buông (c=true) cả hai vẫn `applySite(true)` commit + autosave. `buildPathZoneBody` +param `flatIdx` (xoay live nhắm đúng mesh); `_siteTuneGuiCtx` +2 callback. **Honest:** Frame W/D path vẫn dùng `layerSlider` chung (applySite — như surface zone, chưa tách); rebuild zone-only re-run Poisson mỗi frame (rẻ, ~20 phiến). Playbook `lat-da.md` §2/§5 cập nhật. Gate: archplan tsc + eslint sạch + check-perf 0-error + Vite 7.15s. **Chờ verify :3002** (kéo Rotate° / R / Gap path → KHÔNG tụt fps dù có hồ nước bật). |
| 2026-06-09 | **🪨 Path-zone — thêm Form (chữ-nhật\|tròn) + Xoay + Move (body-drag).** NgQuan: "thêm xoay path + move cho path + chia 2 phần hình chữ nhật và hình tròn như hiện nay". **(1) Form Chữ-nhật\|Tròn:** module `StoneScatter` 1.0→**1.1** +option `shape:'rect'\|'circle'` (circle = lọc tâm Poisson trong ellipse NỘI TIẾP `(x/hw)²+(z/hd)²≤1`; map ALL rồi filter → rng đều, đổi rect↔circle giữ layout chỉ bớt phiến). Render `addStonePathMesh` truyền `shape` reuse `GroundLayer.shape` (đã có); GUI `pathFormRow` (Chữ nhật\|Tròn) reuse shape; `zoneKindRow` đổi Type→path reset shape về 'rect' nếu đang ellipse/free. **(2) Xoay:** `StonePathParams.rot` (độ, -180..180) + parse; `addStonePathMesh` `mesh.rotation.y=rot·π/180`; GUI slider `Rotate°` trong `pathSliderSpecs`. **(3) Move body-drag:** path TRƯỚC kéo = nhảy về gốc vì `_layerDragMove` giả định geo world-baked (mesh.position=0+Δ) nhưng StoneScatter để offset+baseY Ở mesh.position. Fix: `_layerDrag` lưu `startMeshPos=mesh.position.clone()`; move = `startMeshPos + Δ` (giữ Y baseY); `_commitLayerDrag` trừ startMeshPos (path khỏi cộng-đôi offset). **Surface zone KHÔNG đổi** (startMeshPos=0 → y hệt cũ). **Honest:** Move path phải click TRÚNG 1 viên đá (raycast InstancedMesh, khe không bắt; chưa pick-plane); xoay → grass-exclude giữ bbox axis-aligned (`layerRect` rot:0, hở góc nhẹ); circle non-vuông = ellipse. Playbook `playbooks/lat-da.md` cập nhật. Gate: module validate (tsc strict) + archplan tsc + eslint sạch + check-perf 0-error + Vite 10.05s. **Chờ verify :3002** (zone Type=Path → Form Tròn → đá rải trong vòng; Rotate° xoay; Move-drag click đá dời cả khung). |
| 2026-06-08 | **🪨 Lối đi lát đá — TÁI CẤU TRÚC: path = 1 LOẠI zone trong G-level (bỏ tab Path + stoneFields[]).** NgQuan: "bỏ path vào bên trong G1 — mỗi khi tạo G mới, phần z1 có 2 loại: ① z1 thông số cũ (Surface), ② path vừa làm; đều thuộc G mới". Gộp path THÀNH zoneKind của zone add (KHÔNG còn tab riêng/`stoneFields[]`). **State (`state.ts`):** bỏ `StoneFieldConfig`/`stoneFields[]`/`makeStoneField`/`parseStoneField(s)`/`renderStoneFields`; thêm `GroundLayer.zoneKind?: 'surface'|'path'` (optional → 'surface' backward-compat) + `path?: StonePathParams` (rMin/rMax/ellipseMin/gap/thickness/seed/color/material) + `makeStonePathParams` + `parseStonePathParams` (clamp, rMin≤rMax). **Khung = CHÍNH rect zone** (length×width=frame, offsetX/Z=vị trí) — không field riêng. **Render (`fromState.ts`):** bỏ buildStoneFields top-level + SiteHandle.stoneFields + stoneFieldRect (path-zone né cỏ qua `zoneRects` sẵn có); `addZoneMesh` rẽ `zoneKind==='path'` → `addStonePathMesh` (StoneScatter frame=length/width, Y=baseY level; `userData.stonePath` = ref; pad phẳng terrain qua zoneRects); `buildLevelZones` path KHÔNG tính maxTh (không dày stacking). **GUI (`gui/site.ts`):** bỏ tab Path (buildStoneDomain…); `buildZonePane` (op='add') thêm `zoneKindRow` (selectRow **Type: Surface|Path**) → path: `buildPathZoneBody` (Frame W/D + `pathSliderSpecs` R min/max·Ellipse·Gap·Thick·Seed + Material + Color) / surface: `buildSurfaceZoneBody` (cũ); đổi Type → `rebuild(flatIdx)` + applySite. **Lab (`ArchPlanLab.ts`):** bỏ `_siteStoneFields` + tuneStoneField/applyStoneFieldsLive (path edit route qua applySite như slider surface zone); `_rebuildGroundLayersLive` xử mesh `userData.stonePath` → dispose QUA `field.dispose` + rút siteShaders (né double-dispose geo + leak mat); `_collectPathTexKeys` (tách rule complexity) → texture đá path-zone DÙNG CHUNG cache border hồ/RockCluster. **Honest:** path edit = full rebuild on drag (như surface zone, KHÔNG live-tune riêng — rẻ vì 1 draw); đá nằm pad PHẲNG (bám-gò-zone = polish); cỏ né CẢ khung (cỏ-tuft khe = polish); save Phase B `stoneFields[]` (cùng ngày) bỏ qua. Playbook `playbooks/lat-da.md` cập nhật. Gate: module tsc + archplan tsc + eslint (chỉ 3 fence cố hữu) + check-perf 0-error + Vite 4.85s xanh. **Chờ verify :3002** (Ground → G1 ＋ → zone Z1 → Type=Path → đá rải; kéo Frame/R/Gap/Seed; Material đá). |
| 2026-06-08 | **🪨 Lối đi lát đá Phase B — tab Path (ráp StoneScatter vào archplan) cạnh Rock.** NgQuan: "bãi cỏ G0 → tạo 1 mảng G1 vuông VÔ HÌNH bên trong rải các mảng đá tròn/ellipse random ko đụng nhau". **Kiến trúc:** `site.stoneFields[]` + tab Path (mirror `rocks[]`/tab Rock — cô lập, dispose sạch), KHÔNG nhét vào `groundLayers` (path render zone đã rối drape/cut/level → "Surgical Changes"). "Khuôn G1 vô hình" = StoneField instance (offset + frameW/D, không vẽ ra). **State (`site/state.ts`):** `StoneFieldConfig` (enabled + offsetX/Z + frameW/D + rMin/rMax + ellipseMin + gap + thickness + seed + color + material) trong `SiteState.stoneFields?[]` (optional → backward-compat []); `renderStoneFields` + `makeStoneField` (seed ngẫu nhiên) + `parseStoneField(s)` (clamp, rMin≤rMax, tolerant ≤16). **Render (`fromState.ts` `buildStoneFields` EXPORT):** mỗi khuôn bật → `new StoneScatter` (mm→m) → mesh tại `(offsetX/1000, groundThick/1000 + heightAt(hf,x,z), offsetZ/1000)` — BÁM cao-độ gò ở TÂM khuôn; `userData.stoneFieldIdx` + push `ctx.shaders`; trả `SiteHandle.stoneFields`. Cỏ né: `stoneFieldRect` (cả khuôn) vào `siteGrassExclude` → blade cao KHÔNG đâm xuyên phiến dẹt (grassBuildSig tự đổi → re-scatter). **GUI (`gui/site.ts`):** sub-tab **Path** index 5 cạnh Rock — instance `P1│P2│＋` (`buildStoneDomain`/`buildStoneTabs`/`buildStonePane` + `stoneSliderSpecs` data-driven: Frame W/D · R min/max · Ellipse · Gap · Thick · Seed) + ✕ remove + Enable. **Live:** Pos X/Z + Color = `tuneStoneField` (mesh.position/setColor, KHÔNG rebuild); structural = kéo `applyStoneFieldsLive` (rebuild CHỈ stone meshes, **né water-RTT**) / buông `applySite`. **Lab (`ArchPlanLab.ts`):** `_siteStoneFields` zip cfg↔field (từ `h.stoneFields`) + `_tuneStoneField` + `_rebuildStoneFieldsLive` (dispose field cũ khỏi siteShaders → `buildStoneFields`) ← mirror `_rebuildRocksLive`; `_usedBorderTexKeys` +loop renderStoneFields → **texture đá DÙNG CHUNG cache border hồ/RockCluster** (triplanar). **Honest:** Pos live dời X/Z (Y giữ, bám-gò chuẩn lại khi buông); khuôn lớn trên sườn gò = 1 cao-độ tâm (per-stone drape = polish); cỏ-tuft trong khe phiến chưa có (exclude CẢ khuôn → đá trên nền xanh phẳng; per-stone exclude = polish); kéo-thả 3D = hoãn. Playbook `playbooks/lat-da.md`. Gate: module tsc + archplan tsc + eslint (chỉ 3 fence cố hữu) + check-perf 0-error + Vite 6.73s xanh. **Chờ verify :3002** (Ground → Path → ＋ → khuôn đá rải hiện trên cỏ; kéo Frame/R/Gap/Seed → đổi live; Material → texture đá). **Polish kế:** per-stone grass-tuft trong khe · per-stone drape gò · kéo-thả 3D · Voronoi crazy-paving (đích xa). |
| 2026-06-08 | **🪨 Lối đi lát đá Phase A — module `components/StoneScatter` (rải đá tròn/ellipse Poisson-disk).** NgQuan quay lại mảng Ground: "1 mặt phẳng lối đi bằng các mảng trên cỏ trong 1 khuôn vuông"; chốt mô hình **G0 cỏ → G1 khuôn VUÔNG VÔ HÌNH → rải mảng đá tròn/ellipse random KHÔNG đụng nhau** (module-first như RockCluster). **Vênh đã xử:** nút chọn "Voronoi ghép-khít" NHƯNG lời gõ "tròn/ellipse không chạm" (×2) → chốt **v1 = đá rải THƯA có khe** (Voronoi crazy-paving = đích xa). **Thuật toán:** **`poissonDisk`** = Bridson 2007 Fast Poisson-disk trong rect (lưới gia tốc cell=minDist/√2, O(n)) → tâm **blue-noise** (rải đều ngẫu nhiên, không vón cục/không lưới); **`mulberry32`** PRNG deterministic theo seed (0 dep); mỗi tâm gán `r∈[rMin,rMax]` + aspect **ellipse** + xoay Y. **KHÔNG chạm = toán đảm bảo:** bounding-circle mỗi phiến ≤ rMax (rz=r·aspect≤r) ⇒ `minDist=2·rMax+gap` → khe mép-mép ≥ gap>0 (kể cả ellipse xoay). **11 props** (frameW/D + rMin/rMax + ellipseMin + gap + thickness + radialSegments + seed + color + material-bơm) + `setColor` live + `getStoneCount`/`getPlacements` (LOCAL, cho grass-exclude/picking Phase B) + `getTriangleCount`. **Hình:** đĩa CylinderGeometry đơn-vị → **InstancedMesh** scale (rx,dày,rz) per-phiến = **1 DRAW**; material NỘI BỘ flat HOẶC bơm material NGOÀI (texture đá triplanar, caller-owned — sẵn dùng chung cache border hồ/RockCluster). **4 file** (index/example/meta/README) + catalog. **Budget:** 4×4m mặc định ≈ ~20 phiến × 64 tri ≈ **1.3k tri, 1 draw**; cap MAX_STONES=400. **Module rời, KHÔNG đụng archplan** (ráp = Phase B). Gate: `validate.js` PASS (tsc strict, no any, meta đủ) + eslint 0-lỗi (sau prettier-fix) + check-perf 0-error (Cylinder có sẵn uv). **Phase B** = ráp archplan (khuôn G1 vô hình = zone trên cỏ G0 + GUI tune + bám gò + texture đá). |
| 2026-06-08 | **🪨⏸️ Non bộ — DEFERRED LẠI (procedural "chưa ra dáng").** NgQuan sau khi xem: "nhìn chưa ra dáng lắm, để dự án non bộ vào deferred". **Quyết định:** DỪNG non bộ sau Phase A+B (module `RockCluster` + tab Rock + texture đá — đã build & commit, **CODE GIỮ, dùng được**); KHÔNG làm Phase C (preset/rêu-slope) hay polish thêm. **Vì sao "chưa ra dáng":** procedural merged-icosa displaced + flatShading → cụm **bướu TRÒN lởm chởm ĐỀU**, thiếu chất non bộ thật (vách dựng đứng · đá DẸT xếp TẦNG/stratification · khe sâu · overhang · rêu bám). MVP stylized rẻ ≠ karst thật (đã cảnh báo từ đầu, nay xác nhận bằng mắt). **Revisit (đổi PARADIGM, KHÔNG polish bản cũ):** (1) asset đá sculpt (Blender/ZBrush→GLTF) · (2′) generator đá-dẹt-xếp-tầng + erosion/crevice + vách đứng · (3) SDF/marching-cubes (overhang/hang thật). Doc: `deferred/systems/non-bo-rockery-builder.md` (status ⏸️ + 3 đường) · `playbooks/non-bo.md` (status seed + banner deferred). **RockCluster/tab Rock vẫn là khối tái dùng** (đá điểm-nhấn rời, hồ-viền-đá…) — chỉ DỰ ÁN non bộ hoãn. |
| 2026-06-08 | **🪨 Non bộ — TEXTURE đá (triplanar) cho cụm đá, DÙNG CHUNG cache với border hồ.** NgQuan: "thêm texture giống của border water cho rock luôn". **Module (`RockCluster` 1.0→1.1):** +option `material?: THREE.Material` (caller-owned) — bơm → đá dùng material đó thay flat (`flatMat` chỉ tạo khi KHÔNG bơm; `dispose` chỉ đụng `flatMat`, material ngoài KHÔNG dispose; `setColor` no-op khi textured). **State (`site/state.ts`):** `RockConfig.material: BorderMaterialKey` ('none' | icelandic-jagged | coal-stone | rock-rough — REUSE type border hồ) + `parseBorderMat` + default 'none'. **Render (`fromState.ts` `buildRocks`):** `mat = r.material !== 'none' ? opts.borderMatByKey?.[r.material] : undefined` → RockCluster; chưa load/'none' → flat. **Lab (`ArchPlanLab._usedBorderTexKeys`):** +loop `renderRocks` (material≠none) → texture đá load 1 lần/key, **DÙNG CHUNG `_borderTex`/`borderMatByKey`/TexturedSurface triplanar với border hồ** (world-space → 1 material/key xài cả 2). **GUI (`gui/site.ts`):** `ROCK_MAT_OPTS` + `selectRow('Material')` trong buildRockPane → applySite (rebuild đổi material). **Honest:** textured = triplanar tile BORDER_TILE 0.5m (hợp đá cuội nhỏ; đá lớn dùng chung scale — chấp nhận, chỉnh tile riêng = sau); Color picker no-op khi textured (như borderColor hồ). Gate: module tsc + validate PASS + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 4.96s xanh. **Chờ verify :3002** (Rock → Material → Icelandic/Coal/Rock → đá phủ texture đá; chung texture với rào hồ). |
| 2026-06-08 | **🪨 Non bộ Phase B — tab Rock (ráp RockCluster vào archplan) cạnh Water.** NgQuan: "làm 1 tab riêng cho nó trong Ground cạnh water". **State (`site/state.ts`):** `RockConfig` (enabled + offsetX/Z + footprintRadius/height + rockCount/craggy/rockScale/detail/seed + color) trong `SiteState.rocks?[]` (optional → backward-compat []); `renderRocks` (enabled) + `makeRock` (seed ngẫu nhiên → mỗi cụm khác) + `parseRock`/`parseRocks` (clamp khớp slider, int hoá; tolerant ≤16). **Render (`fromState.ts` `buildRocks` EXPORT):** mỗi cụm bật → `new RockCluster` (mm→m) → mesh tại `(offsetX/1000, groundThick/1000 + heightAt(hf,x,z), offsetZ/1000)` — **BÁM cao-độ gò ở TÂM cụm** (hf = `buildHeightField` khi terrain bật); `userData.rockIdx` + push `ctx.shaders` (cluster.dispose lo geo+mat); trả `SiteHandle.rocks`. **GUI (archplan `gui/site.ts`):** sub-tab **🪨 Rock** index 4 cạnh Water — instance `R1│R2│＋` (`buildRockDomain`/`buildRockTabs`/`buildRockPane` + `rockSliderSpecs` data-driven) + ✕ remove + Enable. **Live:** Pos X/Z + Color = `ctx.tuneRock` (mesh.position/setColor, KHÔNG rebuild); structural = kéo `ctx.applyRocksLive` (rebuild CHỈ rock meshes, **né water-RTT** = tụt fps) / buông `applySite`. **Lab (`ArchPlanLab.ts`):** `_siteRocks` zip cfg↔cluster (từ `h.rocks`) + `_tuneRock` + `_rebuildRocksLive` (dispose cụm cũ khỏi siteShaders → `buildRocks` lại) ← mirror `_rebuildGroundLayersLive`; ctx `tuneRock`/`applyRocksLive`. **Honest:** Pos live dời X/Z (Y giữ, bám-gò chuẩn lại khi buông); kéo-thả 3D đặt cụm = HOÃN (slider trước, như mound 3a→3b); cỏ chưa né footprint đá (đá thưa, đế craggy che). Playbook `playbooks/non-bo.md`. Gate: module tsc + archplan tsc + eslint (chỉ 3 fence cố hữu) + check-perf 0-error + Vite 4.96s xanh. **Chờ verify :3002** (Ground → Rock → ＋ → cụm đá hiện; kéo Pos/Count/Craggy/Color → đổi live; bật terrain → đá bám gò). **Phase C:** preset 1-nút "Non bộ" + triplanar texture + rêu-slope. |
| 2026-06-08 | **🪨 Non bộ Phase A — module `components/RockCluster` (đá mỏm procedural).** Mảnh "đá núi" THIẾU để ráp cảnh non bộ (terrain mound = ĐẾ, RockCluster = ĐÁ; deferred `non-bo-rockery-builder.md` DUYỆT: procedural rock-cluster, module-first). **Thuật toán:** **`fbm3`** = value-noise 3D tự-chứa (hash lattice + trilinear smootherstep, 3 octave, ~45 dòng, **0 dependency** — Simplicity); **`icosaRock`** = `IcosahedronGeometry(r,detail)` non-indexed, DISPLACE mỗi đỉnh dọc pháp-tuyến-cầu `craggy·r·fbm3(pos·freq+seedOff)` (displacement CHỈ là hàm vị-trí ⇒ đỉnh trùng-vị-trí dịch giống → KHÔNG nứt mặt); **xếp MỎM** N viên xoắn-ốc GOLDEN_ANGLE (đế rộng `ringR=footprint·(1−t)` → đỉnh hẹp, cao `y=height·t^0.8`, viên nhỏ dần + dẹt Y + xoay deterministic), bake transform → **`mergeGeometries` 1 mesh** flatShading faceted (như `pondStoneGeos`, đã chạy WebGPU). **8 props** (footprintRadius/height/rockCount/craggy/rockScale/detail/seed/color) + `setColor` live + `getTriangleCount`. **4 file** (index/example/meta/README) + catalog README. **Budget:** merged → **1 draw**; detail=2 × 22 viên = **7040 tri**, cap rockCount 60. **Giới hạn:** KHÔNG overhang/hang thật (đá xếp chồng → khe+craggy, không đục hang; height-field cũng không làm được — cùng giới hạn [[character-terrain-follow]]); MVP chấp nhận silhouette "hòn non bộ". **Module rời, KHÔNG đụng archplan** (ráp = Phase B). Gate: `validate.js` PASS (tsc strict, no any, meta đủ) + eslint 0-lỗi (sau prettier-fix) + check-perf 0-error + smoke node (fbm3 ∈ [-0.93,0.92], 0 NaN, deterministic). **Phase B** = ráp archplan (RockCluster trên mound + hồ + rêu + GUI/preset); **Phase C** = polish triplanar rock-texture + rêu-slope. |
| 2026-06-08 | **🏔️ Terrain — DRAPE = "LOANG LỔ" (cát phủ lổ-đổ lên cỏ): biến xuyên-G0 thành TÍNH NĂNG.** NgQuan: "vẫn xuyên thôi, ta để nó như tính năng đi — kiểu cát phủ loang lổ lên cỏ; khi nào cần tôi tắt bám-gò để G1 đè trực tiếp G0". Xuyên-G0 (lưới zone≠G0, không trị tuyệt-đối rẻ) → embrace thành hiệu ứng. **(`fromState.ts`):** **2 chế độ zone:** (a) **DRAPE ON = LOANG LỔ** (`loangLo=true`): zone (cát) **dao động QUANH** mặt-G0-MESH (cỏ) theo gò CENTERED — `g0Mesh + (heightAt(zhf) − ½amplitude)` → offset ∈ ±amp/2: chỗ **chìm** dưới G0 = cỏ ló, chỗ **nổi** = cát ⇒ patches HỮU CƠ (FBM; amplitude=tương-phản, frequency=cỡ-mảng). KHÔNG taper/min-gap (interpenetrate CHÍNH LÀ hiệu ứng); drape không-gò-riêng → `+GAP` (clean bám G0). (b) **DRAPE OFF = ĐÈ TRỰC TIẾP** (`loangLo=false`): gò trên nền G0-đã-mask-phẳng + taper mép + min-gap (clean pad). `zoneSurfaces` trả thêm `loangLo`; `drapedLayerGeometry` rẽ yAt qua `zoneYAtFn` (tách giữ complexity ≤10): loangLo→`surf.top` thẳng / else→taper. g0MeshHeightAt giữ (mốc G0 chuẩn). **Honest:** z-fight chỉ ở ĐƯỜNG-GIAO zone↔G0 (mảnh 1D, chấp nhận); mảng cát-cỏ = geometry interpenetrate (chưa phải material-blend mép). Thuần site-kit (fromState), KHÔNG đụng Lab/gitlink. Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 6.3s xanh. **Chờ verify :3002** (zone Drape + gò → cát/cỏ loang lổ; tắt Drape → đè phẳng clean). |
| 2026-06-08 | **🏔️ Terrain fix — zone drape HẾT XUYÊN G0 khi gò-zone thấp (g0-mesh-height interp).** NgQuan: "G1 vẫn xuyên G0 mỗi khi kéo amplitude thấp xuống". **Root cause:** zone `below` = `heightAt(G0)` (đường-cong THẬT) NHƯNG mặt G0 render là **lưới TAM-GIÁC (facet)** → lệch true-curve-vs-facet ở GIỮA ô (tới ~½ biến-thiên-cao-độ-trong-ô). Gò-zone CAO → zone nổi cao, lệch bị che; gò-zone THẤP (amplitude thấp) → zone ôm sát G0 chỉ 2cm GAP → lệch (>2cm) LỘ → xuyên. **Fix (`fromState.ts`):** **`g0MeshHeightAt(g0,x,z,hw,hd,res)`** — nội-suy TAM-GIÁC trên lưới G0 khớp ĐÚNG `pushGridQuad` (T1 `(00,01,10)` khi `fx+fz≤1` / T2 `(11,10,01)` khi `>1`) → `zoneSurfaces.below` dùng cao-độ **G0 MESH** (facet) THAY heightAt true-curve ⇒ below = mặt G0 render đúng ở đỉnh zone → zone LUÔN = G0 + GAP (hết xuyên). `DrapeCtx` +`hw/hd/res` (lưới G0); `buildGroundLayers` bơm. **Honest:** residual giữa-đỉnh (lưới zone bbox-based ≠ lưới G0 lot-based → zone-edge cắt qua tam-giác G0) = bậc-2 nhỏ (G0 facet trong span 1 ô), GAP 2cm trùm; muốn tuyệt-đối = grid-align zone↔G0 (defer). Thuần site-kit (fromState), KHÔNG đụng Lab/gitlink. Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 4.7s xanh. **Chờ verify :3002** (zone drape + gò-zone amplitude thấp → KHÔNG xuyên G0). |
| 2026-06-08 | **🏔️ Terrain fix — G1 Y LUÔN > G0 Y (không bao giờ bằng) + PERF zone-slider (hết tụt fps).** NgQuan: (a) "Y mọi điểm G1 tuyệt đối không bao giờ bằng Y G0 theo chiếu vuông góc"; (b) "kéo slider G1/G sau tụt fps". **(1) Min-gap (`fromState.ts`):** const `ZONE_MIN_GAP=0.02` (2cm) — mép taper rủ về `below + GAP` (KHÔNG tới `below`) + `zoneSurfaces.top = below + GAP + gò` → **Y zone LUÔN ≥ Y-G-dưới + 2cm** ⇒ G1 ≠ G0 mọi (x,z) (né z-fight + trùm sai-số nội-suy lưới curve-vs-chord; gò dốc cực đoan → grid-align defer). **(2) Perf:** kéo slider terrain-zone xưa = `applySiteLive` → `_renderSite/_rebuildSite` (siteSig đổi) → **tái-tạo water reflector RTT + recompile NodeMaterial mỗi frame** = tụt fps. Fix: zone slider live → **`applyTerrainLive`** (mở rộng): swap base G0 (như cũ) + **`_rebuildGroundLayersLive()`** (gỡ+dispose mesh zone cũ `userData.groundLayerIdx` khỏi siteGroup + `buildGroundLayers(clean=false)` cell-drop) — **KHÔNG đụng nước/cỏ/material-cache** (= chỗ tụt fps). `drapedLayerGeometry` +param `clean`: **commit**=clip ô-biên `polygonClipping` (mép sạch) / **live**=bỏ-ô tâm-trong-contour (`emitZoneQuad`, né clip Martinez = mượt). `buildGroundLayers` **EXPORT** + `clean=true` default + thread qua buildLevelZones/addZoneMesh. cut-patch own-material dispose (`_disposeSiteMat`) tránh leak lúc live. Buông → `_applySite(true)` clip sạch. Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 4.9s xanh. **Chờ verify :3002** (kéo slider gò zone G1+ → mượt fps; zone luôn nổi trên G0 không z-fight). |
| 2026-06-08 | **🏔️ Terrain fix — mép zone displaced HẠ THẤP TỰ NHIÊN (edge taper, hết lơ lửng).** NgQuan: zone bật gò/drape (mặt-đơn) → mép HỞ, lơ lửng trên mặt-G-dưới. Chốt KHÔNG dùng váy/skirt vuông góc mà **hạ thấp tự nhiên** rủ xuống chạm G dưới. **Fix (`fromState.ts`):** `zoneYAt`→**`zoneSurfaces`** trả `{top, below}` (`below` = mặt-G-NGAY-DƯỚI = `baseY + gò-G0`, KHÔNG gò-zone/5mm = đáy taper); `drapedLayerGeometry` đổi nhận `surf` + **`yAt = below + smoothstep(distToPolyEdge(x,z)/band)·(top−below)`** → ở VIỀN (dist→0) yAt=`below` (chạm G dưới), trong LÒNG = `top` đầy gò. `band` = `layer.terrain.edgeFlat` (mặc định 800mm, cap ≤45% cạnh nhỏ zone). Thêm `distToPolyEdge`/`distPointSeg` (điểm→cạnh contour). Mép RỦ MƯỢT (smoothstep) theo chiếu-đứng-xuống. **Đơn giản hơn skirt** (chỉ sửa hàm Y, KHÔNG thêm geometry tường). **Honest:** chỉ taper viền NGOÀI (holes/cut chưa — refine sau); band-theo-edgeFlat (zone có terrain) / 800mm (drape-only). Thuần site-kit (fromState), KHÔNG đụng Lab/gitlink. Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 6.1s xanh. **Chờ verify :3002** (zone bật gò → mép rủ mượt xuống nền, không lơ lửng). |
| 2026-06-08 | **🏔️ Terrain — GÒ RIÊNG mỗi zone (G1/G2/z1/z2 có noise+mounds độc lập G0).** NgQuan: "cho tính năng gò sân vườn vào các zones". **State:** `GroundLayer.terrain?: TerrainConfig` (parse reuse parseTerrain, backward-compat undefined). **Geometry (`fromState.ts`):** refactor `GridBuild`→tách `GridEmit` dùng hàm **`yAt(x,z)`** (thay `hf`+`topY` cứng) → `emitWorldTri` + grid-loop + `drapedLayerGeometry` DÙNG CHUNG. **`zoneYAt(layer,baseY,drape)`** tổ hợp cao-độ: `baseY+5mm + (heightAt gò-G0 nếu drape&G0-terrain) + (heightAt gò-RIÊNG-zone nếu layer.terrain.enabled)`; gò-zone = `makeHeightField(layer.terrain, [], 1e6,1e6)` (mask off → fill cả zone, clip theo contour ở geometry; `Math.max(dy,0)` → gò dương). `zoneCell` = cell-G0 (drape) hoặc zone-size÷resolution (gò-riêng). `addZoneMesh` rẽ `zoneYAt` (null = slab phẳng ExtrudeGeometry). **Hoạt động CẢ khi G0 terrain TẮT** (zone tự undulate). **Trực giao Drape:** `drape`=bám-gò-G0, `terrain`=gò-riêng-zone → cộng dồn (4 tổ hợp). **GUI:** parameterize **`buildTerrainControls(body,ctx,t,applyLive,refresh,showDetail)`** + `buildMoundControls`/`buildMoundRow` (+applyLive) → reuse cho zone trong **`buildAddZoneExtras`** (live=`applySiteLive`, showDetail=false vì per-zone detail = Feature vùi-lấp KẾ). G0 caller cập nhật (applyTerrainLive, showDetail=true). **Honest:** cỏ vẫn theo height-field G0 (KHÔNG theo gò-riêng-zone — niche, grass-over-zone vốn hiếm); mounds Pos theo world (lot center); zone terrain slider live = `applySiteLive` (throttle re-render, không có geo-swap riêng như G0). Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 5.1s xanh. **Chờ verify :3002** (G1 zone → section Terrain riêng → Enable + slider/＋gò → zone undulate gò riêng). **Feature KẾ: toggle "vùi lấp" per-zone** (bật/tắt detail-normal riêng zone — cần material-variant per-zone). |
| 2026-06-08 | **🏔️ Terrain — ZONE DRAPE (G1/z1/z2 uốn BÁM gò) + toggle Phẳng-pad\|Drape per-zone.** NgQuan: muốn zones "phủ lên theo G0" (thay vì làm-phẳng gò dưới chúng). **State:** `GroundLayer.drape?` (parse backward-compat false). **2 chế độ/zone:** (a) `drape=false` (mặc định) = slab PHẲNG + gò làm-phẳng-dưới (mask "pad"/patio — fix trước đó); (b) `drape=true` = zone UỐN theo gò. **Geometry (`fromState.ts` `drapedLayerGeometry`):** lưới `gridCell`(=cạnh-lô/resolution, KHỚP mật độ nền G0) trên bbox zone → mỗi ô `polygonClipping.intersection(ô, contour) − holes(cut/nước)` → `THREE.ShapeUtils.triangulateShape` (gồm lỗ-trong-ô) → **`emitWorldTri` Y = baseY+5mm+heightAt** (đậu trên gò như nền). Mặt ĐƠN thin-sheet (bỏ thickness — chỉ dùng stacking level). **Refactor:** `GridBuild` → tách `GridEmit` (hf/topY/pos/uv/idx) để `emitWorldTri` DÙNG CHUNG nền G0 ↔ zone drape (Rule-of-2 sắp 3). **`zoneRects` lọc chỉ drape=false** (drape=true KHÔNG vào mask → gò giữ nhấp nhô để zone bám); cỏ `buildSiteGrass` cùng `zoneRects` → khớp. **`addZoneMesh` rẽ nhánh** flat(ExtrudeGeometry+translate) / drape(drapedLayerGeometry, Y đã bake) + thread **`DrapeCtx{hf,cell}`** qua buildLevelZones/buildGroundLayers (hf dựng 1 lần/rebuild). **GUI:** toggle "Drape (bám gò)" mỗi add-zone (`buildZonePane`, structural→applySite). **Honest:** drape = mặt đơn (không hiện thickness-edge); zone clip mọi ô lúc COMMIT (zone lớn chậm hơn chút, KHÔNG per-frame — terrain drag chỉ swap base, zone re-drape khi buông). cut/free-shape: clip theo contour đúng mọi shape. Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 11.9s xanh. **Chờ verify :3002** (terrain bật + G1 zone → bật Drape → zone uốn theo gò; tắt Drape → phẳng pad, gò né). |
| 2026-06-08 | **🏔️ Terrain fix — gò KHÔNG "cướp đất" zones (G1/G2/z1/z2).** NgQuan chỉ: gò ngẫu nhiên ở G0 nhấp nhô DƯỚI vùng zones → zone (ExtrudeGeometry slab PHẲNG, `addZoneMesh` translate Y cố định) nổi/lún ở mép (defer Phase-1 "zone phẳng nổi trên gò" → giờ đóng). **Fix (thuần site-kit `fromState.ts`):** thêm footprint zones vào **mask terrain** (cơ chế giữ-phẳng đã có cho nhà/hồ/viền). `layerRect(layer)` = bbox `layerWorldPolygon` (axis-aligned, như waterRect) + **`zoneRects(site)`** = mọi ADD-layer (`op!=='cut'` — cut là lỗ, không surface để cướp). `buildHeightField` (nền) push `...zoneRects`; **`buildSiteGrass` push CÙNG zoneRects** (cỏ KHỚP nền dưới zone, né cỏ-nổi); `grassBuildSig` += `groundLayers` khi terrain bật (zone đổi → mask phẳng-dưới-zone đổi → re-scatter). ⇒ gò giữ PHẲNG dưới mọi zone → zone giữ trọn đất. **Honest:** bbox axis-aligned (non-rect/xoay flatten hơi rộng) + halo `padMargin` quanh zone (như nhà — muốn gò sát mép zone thì hạ padMargin). DÙNG CHUNG zoneRects giữa nền↔cỏ = 1 nguồn. KHÔNG đụng Lab (fromState đọc `site.groundLayers`). Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 6.4s xanh. **Chờ verify :3002** (bật Terrain + có G1/zone → zone phẳng, gò né vùng zone). |
| 2026-06-08 | **🏔️ Terrain Phase 4 — DETAIL MICRO-RELIEF (phá vẻ "nhựa" gò nhẵn) — TERRAIN HOÀN TẤT.** `PhotoGround` (`threejs-modules/shaders/ground/PhotoGround`) +option **`detail`** [0..1] + `detailScale` (freq) + **`setDetail`** (live uniform `uDetail`): perturbation normal PROCEDURAL (`mx_noise_float` finite-diff 3-mẫu gradient world-XZ × freq, verify export `three/tsl` 0.174) **CỘNG lên** normal map (KHÔNG thay) → mặt nền gò bớt "nhựa CG", có vi-relief tần-số-khác texture. `_normalNode` viết lại: map có/vắng (vắng → base +Y) → `_detailNormal` tilt `worldN.x/z`. **Driver = `terrain.detail`** (field đã có từ P1, đang chết → giờ sống): site-kit `groundMaterial` bơm `detail: terrain.detail`; Lab `_groundMatFor` bơm khởi tạo + **`_applyTerrainDetail()`** đẩy uniform vào MỌI PhotoGround cache (`Object.values(_groundMat).setDetail`) — **vượt ranh per-key material-cache** (live, KHÔNG recompile/rebuild). GUI slider **"Detail (sần)"** trong section Terrain — live = `applyTerrainDetail` (uniform, KHÁC slider noise dùng `applyTerrainLive`=geometry), buông = `applySite` persist; +`APGuiCtx.applyTerrainDetail`. **Honest:** CHỈ PhotoGround (ground texture chính); GrassGround/màu-phẳng KHÔNG (procedural sẵn/placeholder). uDetail=0 → no-op visual (vẫn tính noise ~0.1ms — negligible vs reflector RTT). Giá-trị MỎNG (grounds đã có normal map) — polish cận cảnh; macro-shape vẫn do geometry. meta PhotoGround 1.0→1.1. Gate: module tsc + archplan tsc + eslint (chỉ fence cố hữu) + check-perf 0-error + Vite 5.9s xanh. **Chờ verify :3002** (Ground → texture → Terrain → slider Detail → mặt sần micro-relief live). **🏔️ TERRAIN PHASE 1–4 HOÀN TẤT** (gò noise + cỏ bám + nặn GUI/3D + detail-normal). |
| 2026-06-08 | **🏔️ Terrain Phase 3b — NẶN GÒ 3D VIEWPORT (MoundTool: kéo tâm + bán kính).** `archplan/interaction/moundDrag.ts` **MoundTool** (mirror GroundTool/WaterTool) — MỖI gò hiện 2 handle: **TÂM** (sphere vàng, kéo trên XZ-plane → `mound.x/z`) + **BÁN KÍNH** (sphere cyan ở mép +X, kéo → `radius` = khoảng-cách-ngang tâm→con-trỏ, clamp 0.3–20m khớp parseMounds). Kéo = `applyTerrainLive` (swap geo nền base, rẻ — gò theo con trỏ) · buông = `commitSite` (rebuild + cỏ né lại + autosave); **đĩa footprint** mờ (ShapeGeometry, `depthTest:false`) hiện lúc kéo để định vị. Handle `depthTest:false` + renderOrder cao → luôn thấy/grab dù terrain che. Chỉ hiện khi **terrain.enabled + moveMode**. **Honest:** Height/Falloff GIỮ slider 3a (vertical-drag phụ-thuộc-góc-camera → nhiễu; slider chính xác, đúng pro-tool); tạo gò vẫn nút `＋` (né mode-ambiguity click-đặt). **Wire ArchPlanLab** (8 điểm): import/field/onInit-construct + pointer down (`_tryStartSiteTool`)/move (`_dragSiteTool`)/up (`_endSiteDrag` loop) + `_setMoveMode` (cancel+rebuild loop) + `_rebuildSite` rebuildHandles + `_disposeActors` loop + `_moundHost`. **Refactor complexity (Rule-of-3):** có ≥3 tool site (💧water/🟫ground/⛰️mound) chia sẻ thao tác đồng nhất → gom **`SiteDragTool` interface** + `_siteDragTools()` (derived, filter null) + dispatch **LOOP** ở 5 method (vốn chạm trần-10, mỗi `?.moundTool` +1 → vỡ). Gotcha TS đã trị: type-guard narrow về **union CLASS** (`t is WaterTool|GroundTool|MoundTool`) KHÔNG phải interface — class có private field → predicate `t is SiteDragTool` sai chiều (interface không gán-ngược). Gate: tsc sạch + eslint 0-lỗi (complexity OK sau refactor + autofix import-sort/prettier) + check-perf 0-error + Vite build 10.7s xanh. **Chờ verify :3002** (Move mode + terrain bật → kéo cầu vàng dời gò / cầu cyan đổi cỡ; cỏ + nền theo). Terrain Phase 1–3 HOÀN TẤT (Phase 4 detail-normal = optional sau). |
| 2026-06-08 | **🏔️ Terrain Phase 3a — NẶN GÒ TAY (GUI mounds list + slider).** `archplan/gui/site.ts`: **`buildMoundControls`** (section "⛰️ Gò nặn tay" dưới Terrain) — list mounds + **`＋Thêm gò`** (default tâm-lô R2m cao 40cm, **tự bật terrain** vì `heightAt` early-return khi tắt → gò vô hình) / **`✕Xoá gò`** + **`buildMoundRow`** mỗi gò 5 slider (Pos X/Z · Radius · Height · Falloff) + **`moundSliderSpecs`** (reuse `TerrainSlider`). Slider STRUCTURAL → `applyTerrainLive` (kéo, swap geo) / `applySite` (buông) như slider noise; thêm/xoá đổi LIST → `refresh()` dựng lại GUI. **Ghi `terrain.mounds[]` → `heightAt` cộng `moundsHeightMm`** (Σ gò radial smoothstep, ĐÃ CÓ từ Phase 1) → griddedGroundGeometry + cỏ sample. **KHÔNG đụng lõi** (state.ts `TerrainMound`/`parseMounds` + terrain.ts `moundsHeightMm` sẵn) — Phase 3a thuần UI archplan. **Honest:** height ÂM (lõm) bị grid-floor `Math.max(dy,0)` chặn (chỉ hạ vùng đã cao = cancel noise, KHÔNG thủng base trên nền phẳng — giới hạn cố ý); `parseMounds` clamp (x±50m/R0.1-20m/H±2m/falloff0-1) bao trọn range GUI (save/load an toàn); nút `＋` **inline-style** (né sửa `archplan-lab.css` = vùng Factory roof). Commit archplan `1f60324` (stage CHỌN LỌC `site.ts`, né roof-lab/roof-preview WIP Factory). Gate: tsc + eslint (site.ts 0-lỗi) + Vite build 9.6s xanh. **Chờ verify :3002** (tab Ground → ⛰️ → ＋Thêm gò → kéo Height/Pos/Radius → gò nhô trên sân; cỏ bám gò). **Phase 3b** = nặn 3D viewport (click-drag đặt/dời gò, mirror waterDrag). |
| 2026-06-07 | **🏔️ Terrain Phase 2 — CỎ BÁM GÒ (hết lơ lửng/chìm gốc).** `GrassBlades` +option **`heightAt?: (x,z)=>number`** (Δy nền m): `_scatter` nâng gốc cụm `gy = baseY + heightAt(px,pz)` + `_emitContacts` vệt-tiếp-đất nâng theo (sample TÂM cụm — bụi ~4cm bỏ qua chênh trong cụm); module VẪN ĐỘC LẬP (caller bơm closure, KHÔNG import site). **`buildSiteGrass` (`fromState.ts`):** dựng `makeHeightField(terrain, exclude, lotHalf…)` rồi bơm `heightAt: hf?(x,z)=>heightAt(hf,x,z):undefined` — **mấu chốt: `exclude` cỏ (foundation+hồ) ≡ maskRects nền** (cùng `_foundationRects()`+`waterRect()` ⇒ buildHeightField) nên cỏ ngồi KHÍT mặt nền displaced (không cần truyền opts). `grassBuildSig` +`site.terrain` → đổi terrain RE-SCATTER bám gò mới. **Perf:** `heightAt` chỉ chạy lúc rải (build-time) → **0 cost runtime**; terrain drag = `_applyTerrainLive` (ground-only swap) KHÔNG gọi `_syncGrass` → cỏ giữ nguyên tới lúc buông (commit) mới re-scatter (né rải 24k lá/frame). **Trade-off:** vệt-tiếp-đất nằm ngang ở `heightAt` (KHÔNG nghiêng theo slope — cần normal, defer; gò thoải ít lộ) · cỏ lag 1 nhịp sau ground lúc kéo terrain (snap khít khi buông). Gate: archplan tsc sạch + site-kit tsc sạch + eslint 0-lỗi-mới (4 lỗi pre-existing: 3 fence + 1 prettier contactQuad) + check-perf 0-error. Doc: `playbooks/ground.md` (§Terrain + history) + GrassBlades README (prop `heightAt`). **Chờ verify :3002** (bật Terrain + cỏ → lá bám sóng gò, gốc không lơ lửng/chìm). Phase 3 = nặn gò tay; Phase 4 = detail-normal. |
| 2026-06-07 | **🏔️ Terrain Phase 1 — fix 3: cắt lỗ hồ SẠCH (như cut C1) + 🪞 2+ hồ hết đơ gương (KI-012) + thống nhất khoét lỗ.** **(A) Lỗ hồ trên gò — khoét sạch:** đổi nguồn lỗ `allWaterCarvePolygons`→**`waterPolygons`** (KHỚP path phẳng — hết thủng-dưới-coping + puddle-lơ-lửng; bug `ee4778a`); thay bỏ-nguyên-ô (răng cưa) bằng **CLIP ô-biên `polygonClipping.difference` + `ShapeUtils.triangulateShape`** (mép bám đúng polygon mọi shape, như cut C1) — `griddedGroundGeometry` phân loại ô (xa hồ/0-góc → lưới; 4-góc → bỏ; 1-3 góc → clip), broad-phase `polyBox`, winding +Y per-tri. `groundGeometry(.., clean)`: commit=clip sạch, **live-drag=bỏ-ô nhanh** (né clip Martinez mỗi frame). Verify Three 0.174 `ShapeUtils.triangulateShape` + `polygon-clipping`. **(B) 🪞 2+ hồ đơ gương** (`known-issues/KI-012`): three `ReflectorNode` dùng cờ module-global `_inReflector`+temp (`bounces:false`) — hồ A render RTT lại render mặt nước hồ B → chen nested-render → B đơ (1 hồ ok; tắt terrain vẫn bị; kèm "không ăn bóng"=ảnh đơ cũ). Fix: mặt nước → **layer riêng** + `WaterSurface.excludeReflectionLayer` **disable layer đó trên `virtualCamera` mỗi frame** (mấu chốt: `getVirtualCamera`=`camera.clone()` COPY layers → chỉ set layer mesh+camera là CHƯA ĐỦ) → reflector né mặt nước. Camera+`_ray` enable layer (vẫn thấy/click/kéo). KHÔNG patch three; mất "nước-soi-nước" (vô hại). **(C) Doc thống nhất:** `playbooks/ground.md §2` bảng "Khoét lỗ hồ" (1 nguồn `pondWorldXZ` → base=waterPolygons che-dưới-coping / zone=allWaterCarvePolygons né-coping); KI-012 + pond.md cập nhật. Gate tsc/eslint/check-perf + Vite build xanh. **Verify OK :3002** (mép hồ sạch + 2 hồ gương chạy). |
| 2026-06-07 | **🏔️ Terrain Phase 1 — fix 2: G0 không xấn grid + live-drag hết tụt fps.** **(1) Grid-floor:** `heightAt` BIAS FBM `[-1,1]→[0,1]` (`fbm·0.5+0.5`, heightmap KHÔNG-âm) + `Math.max(dy,0)` → Y=topY+Δy LUÔN ≥ topY (10mm) → hết **lỗ đen lộ void** (FBM dip âm xấn dưới grid; thấy trên ảnh :3002). amplitude = cao đỉnh dune trên base. **(2) Perf:** kéo MỌI slider Terrain tụt fps vì `applySite(false)→_rebuildSite` tái-tạo **water reflector RTT + recompile NodeMaterial** mỗi frame (terrain chỉ đụng ground geo). Tách `_applyTerrainLive` (như `_applyFenceLive`): throttle rAF → `groundGeometry()` build lưới mới → **SWAP `_siteGroundMesh.geometry`** (dispose cũ + update `siteGeos` tracking), KHÔNG đụng hồ/material/cỏ/rào. `SiteHandle` +`ground:Mesh` (caller giữ ref) + `groundGeometry()` export tách khỏi `buildGround`. GUI slider terrain: kéo→`applyTerrainLive`, buông→`applySite(true)`. Gate tsc/eslint/check-perf + Vite build xanh. Doc: ground.md. |
| 2026-06-07 | **🏔️ TERRAIN Phase 1 — nền sân vườn lồi-lõm (height-field procedural).** **Lõi `site/terrain.ts` (MỚI, PURE):** `heightAt(hf,x,z)` = 1-điểm-lan-truyền (mirror `pondWorldXZ`/`shapes.ts`) = **mask · (amplitude·FBM + Σ gò)** ÷1000. FBM = value-noise hash-based seeded (KHÔNG dependency) + lacunarity/gain/octaves + **domain-warp**; mask (rect-based, rẻ) = 0 ở **pad nhà ∪ quanh hồ ∪ viền lô** → smoothstep →1 ở garden ⇒ **móng/hồ/rào/lot-edge GIỮ PHẲNG, KHÔNG đổi**. **State (`state.ts`):** `SiteState.terrain?: TerrainConfig` (enabled/amplitude/frequency/octaves/lacunarity/gain/warp/seed/resolution/padMargin/edgeFlat/detail/mounds) + `parseTerrain`/`defaultTerrain` (optional → backward-compat, KHÔNG bump schema; mặc định TẮT = opt-in). **Render (`fromState.ts`):** `buildGround` rẽ nhánh — terrain bật → **`griddedGroundGeometry`** (lưới `resolution²` displace Y=topY+heightAt, **đục lỗ hồ** = bỏ tam-giác có tâm trong `allWaterCarvePolygons` qua `pointInPolygon`; mặt-trên-đơn, mép răng-cưa khuất dưới coping) + `SiteRenderOpts.buildingFootprint` (mask pad). terrain tắt → path phẳng cũ (`flatGroundGeometry`). Ground shader sample `positionWorld.xz` → texture tự bám gò. **GUI (`gui/site.ts`):** section "🏔️ Terrain" tab Ground (`buildTerrainControls`+`terrainSliderSpecs`) — **10 slider noise ĐẦY ĐỦ** amplitude/frequency/octaves/lacunarity/gain/warp/seed/resolution/padMargin/edgeFlat, STRUCTURAL `ctx.applySite(c)` (kéo=live-rebuild, buông=commit). **Lab:** bơm `_foundationRects()`→`opts.buildingFootprint`; siteSig đã gồm terrain → tự rebuild. **Trade-off:** đục-lỗ-hồ răng-cưa theo grid (khuất coping) · không skirt (viền phẳng+rào che) · zone phẳng nổi trên gò (defer) · live-drag rebuild kèm water-RTT như Thickness. **Gaea = DEFER** (sai scale + phá live-edit; `heightAt` chừa cửa cộng `heightmapSample` cho environment lớn → `deferred/systems/terrain-gaea-heightmap.md`). Gate site-kit tsc/eslint (terrain/state/fromState sạch; chỉ fence cố hữu) + archplan tsc/eslint + check-perf 0-error + **Vite build :3002 xanh (4.7s)**. Doc: `playbooks/ground.md` + deferred. **Phase 2** (cỏ bám gò) chờ user xác nhận. **Chờ verify :3002** (tab Ground → Terrain → Enable + kéo slider → sân gò; nhà/hồ/viền phẳng). |
| 2026-06-07 | **+4 texture đá: cobblestone→ground surface + icelandic/coal/rock→pond border (texture đá rào, triplanar).** **Pipeline (assets):** 4 scan thô (cobblestone/coal_stone/rock_rough/icelandic_jagged) → `source/` + `production/` (jpg + **ktx2** toktx recipe: basecolor ETC1S srgb / normal UASTC RDO linear / rough+ao ETC1S, `--genmipmap --t2`) + meta.json. icelandic KHÔNG có AO map (source thiếu → 3 map). **(B) cobblestone → ground surface:** nhân bản pattern turf — `GroundMaterialKey` +`cobblestone` + GROUND_TEX_KEYS + GROUND_PRESETS (#626263) + archplan GROUND_TEX_SPEC + GUI GROUND_OPTS (PhotoGround world-XZ tile 2m, tự qua `_groundMatFor`/`_ensureGroundTex`). **(C) HỆ BORDER-TEXTURE mới:** state `BorderMaterialKey='none'|icelandic-jagged|coal-stone|rock-rough` + `WaterConfig.borderMaterial` (parse tolerant). Render `buildPondBorder` → `borderMaterialFor`: texture key + `opts.borderMatByKey?.[key]` = **material injected (TexturedSurface triplanar, caller-owned KHÔNG push)**, else màu phẳng borderColor. SiteRenderOpts +`borderMatByKey`. Archplan: BORDER_TEX_SPEC (3 rock, **tile 0.5m** đá nhỏ) + `_borderTex` cache {maps,surf} + `_ensureBorderTex` (load→TexturedSurface→cache→re-render) + `_usedBorderTexKeys` (hồ borderEnabled+≠none) → inject `_siteTexOpts` + dispose `_disposeFenceBorderTex`. GUI `buildBorderRows` +dropdown Border mat (None+3 rock). Triplanar áp được CẢ đá cuội (icosa) lẫn rào gỗ (box). Refactor: tách `_disposeFenceBorderTex` + `borderMaterialFor` (complexity ≤10). Gate site-kit tsc/eslint (chỉ fence cố hữu) + archplan tsc/eslint + check-perf 0-error + validate (kebab false-positive underscore) + Vite :3002 200 (ktx2 served) xanh. Doc: pond.md + ground.md. **Chờ verify :3002** (ground→cobblestone; hồ→Border mat→3 rock đá). |
| 2026-06-07 | **Rào/viền quanh hồ (auto theo shape) + rename Pool edge→Pond edge.** **Render (`fromState.ts` `buildPondBorder`):** chạy dọc VÀNH NGOÀI coping (`offsetPolygon(pondWorldXZ, edgeWidth)`; bờ nước nếu edgeWidth=0). AUTO theo shape: **rect → hàng rào gỗ** (`pondFenceGeos`: cọc box 8cm cách ~1.5m, t=0=góc không lặp + 2 thanh ngang box dài=cạnh xoay `rotateY(−atan2)`, ở h×0.85/h×0.45); **tròn/ellipse/free → ĐÁ CUỘI** (`pondStoneGeos`/`stoneAt`: Icosahedron detail-1 faceted ~80tri, sample arc-length mỗi 0.82×đường-kính = xếp liền chồng nhẹ, jitter scale-ngang 0.78–1.18 + dẹt-Y + xoay-Y **deterministic** `hash01(idx)` né nhảy mỗi rebuild). Merge 1 mesh/hồ (**KHÔNG trộn** box-indexed + icosa-non-indexed trong 1 merge → né KI-004; verify esbuild: stones 2880v / fence 168v non-null, attrs khớp). Material `MeshStandardMaterial` phẳng (đá flatShading, gỗ mượt), cast/receive shadow. Wire trong `buildWater` sau `buildPoolEdge`. **State:** `WaterConfig` +`borderEnabled`/`borderHeight`(100–1200mm)/`borderColor` + parse tolerant + makeWater default (off/350mm/xám-ấm). **GUI (`gui/site.ts`):** `buildBorderRows` ở tab edge (toggle + slider H + colorRow, **commit-only** né tái-tạo reflector RTT lúc kéo); nhãn tab **"Pool edge"→"Pond edge"** theo `w.kind`. Gate site-kit tsc/eslint (chỉ fence cố hữu) + archplan tsc/eslint + check-perf 0-error + Vite :3002 200 xanh. Doc: `playbooks/pond.md`. **Chờ verify :3002** (bật Border ở rect → rào gỗ; tròn/free → đá cuội uốn theo bờ). |
| 2026-06-07 | **Thêm G-level RỖNG (model count tường minh) + ✕ Remove G-level.** Trị "＋G tự sinh Z1": chuyển từ level-DERIVE-từ-layers → **`SiteState.groundLevels` (số tường minh)**. **State (`state.ts`):** +field optional + `parseGroundLevels` migrate `?? max(level layers)` (luôn ≥ max → không ẩn tầng có layer) + default 0; KHÔNG bump schema (như shape/op). **GUI (`gui/site.ts`):** `groundEditorLevels(count)` enumerate tab `G1..GN` (gồm tầng rỗng) thay `groundLevelNums`(derive); **`＋`G = `groundLevels++` (tầng RỖNG, bỏ `makeGroundLayer` push)** + focus tab mới (rebuild thêm param `focusLevel`); **`✕ Remove G` mới** (`removeGLevelRow`) = filter bỏ layer level đó + dồn `level>lv` xuống 1 + `groundLevels--` + clear active (né stale handle) + về G0. **Render KHÔNG đổi** (`groundRenderLevels` vẫn derive từ layers → tầng rỗng = no-op, Y-stacking +0). Refactor `navTo` tách `selectMidInst` (complexity ≤10 sau khi `?? 0` bump). Gate site-kit tsc/eslint (state.ts) + archplan tsc/eslint (gui/site, 0-error; roof = WIP Factory) + Vite :3002 200 xanh. Doc: `playbooks/ground.md` (state model + editor + history). **Chờ verify :3002** (＋G → tầng rỗng → ＋zone/cut riêng; Remove G dồn số). |
| 2026-06-07 | **Add/cut mảng ground FORM TỰ DO (Free bezier) — nắn đỉnh + tay-cầm 3D.** Hoàn tất phần editor cho ground free-form (render contour cong + state `GroundLayer.shape:'free'`+`points` ĐÃ có từ 2026-06-06). **GUI (`gui/site.ts`):** `groundFormRow` +`Free (bezier)` (cả Mảng add lẫn Khoét cut); đổi → Free **seed blob 8-điểm** + tay-cầm Catmull-Rom mirror qua **`rectBezierPoints`** (tách từ `seedWaterRectPoints` — DÙNG CHUNG hồ↔ground, 1 nguồn). **Nắn 3D (`interaction/groundDrag.ts` MỚI — GroundTool, mirror waterDrag):** Move + layer free active → overlay đỉnh (sphere vàng `vi`) + 2 tay-cầm bezier in/out (sphere cyan `hi+which`) + LineSegments nối; `tryStartVertex` raycast sphere → session `vertex`/`handle`; kéo → ghi `points[]`/offset tay-cầm **live** + viền vàng (`showOutline` FILL vẽ-trên-cùng) định-vị; buông = `commitSite` (rebuild đặt lỗ-cut THẬT). Tâm overlay = `offsetX/Z`, cao độ = `boundingBox.max.y` của mesh layer (add=extrude / cut=mảng-xám, tìm qua `groundLayerIdx`). **Lab wiring:** field `_activeLayerIdx` (layer focus add HOẶC cut, set trong `_setActiveCut`) + `_groundHost` + pointer down (ưu tiên tay-cầm sau water, trước body) / move / up (tách `_endSiteDrag` giữ complexity ≤10) + `rebuildHandles` sau render/đổi-active/đổi-moveMode + dispose. **Body-drag (dời layer) GIỮ ở Lab `_layerDrag`** (surgical, không gom). **Trade-off:** live = OUTLINE-khi-kéo + commit-khi-buông (né per-frame polygon-clipping, PERFORMANCE.md); WYSIWYG live-rebuild geometry = defer. Gate archplan tsc/eslint (3 file 0-error; roof-preview lỗi = WIP Factory, không stage) + Vite transform :3002 200 xanh. Doc: `playbooks/ground.md` (recipe Form+GroundTool, history, deferred). **Chờ verify :3002** (Form→Free → Move → kéo đỉnh/tay-cầm nắn cong; cả Z* lẫn C*). |
| 2026-06-07 | **+4 texture ground (artificial_turf, grass_o, thai_beach_sand 2K+4K) vào 3 hệ + WATER-BOTTOM texture (capability mới).** **Pipeline:** 4 scan thô → KTX2 (toktx recipe per-map: basecolor ETC1S srgb / normal UASTC RDO linear / rough+ao ETC1S, `--genmipmap`) + meta.json + production/ (jpg+ktx2). sand4k normal ~20MB (vượt budget 2048 — user chọn 4K full). **3 hệ surface = 3 enum riêng:** (1) **Ground surface** lô/vườn — `GroundMaterialKey` + GROUND_TEX_KEYS + GROUND_PRESETS + `GROUND_TEX_SPEC` + GROUND_OPTS (pattern sẵn). (2) **Bộ nền** editor-backdrop — `GroundType` +3 (trùng tên GroundMaterialKey) + method **generic `_photoEditorGround`** TÁI DÙNG `_groundMatFor` cache PhotoGround (share material world-XZ site↔backdrop) + phím **5/6/7** + hook re-apply khi `_ensureGroundTex` xong. (3) **Water bottom** (MỚI) — `WaterMaterialKey = 'none'\|'tile'\|GroundMaterialKey`; `basinMaterial` nhận **material injected** (`groundMatByKey`, đáy basin uv=world-XZ → lát khớp, KHÔNG push/dispose như `resolveGroundMat`); thread `opts` qua buildWater→buildBasin; `_usedGroundTexKeys` gom thêm floor/wall key → tự load+inject; GUI Bottom▸**Floor/Wall mat** +3 texture. KHÔNG đụng component WaterSurface (chỉ basin trong fromState). **Perf:** 4K loại khỏi `_prefetchGroundTextures` (mở dropdown KHÔNG kéo 27MB; chỉ tải khi CHỌN — lazy + cache hash-immutable). Gate site-kit eslint (chỉ fence cố hữu) + archplan tsc/eslint + check-perf + validate (kebab false-positive textures + sand4k over-budget = user override) xanh. **Chờ verify :3002.** Nguyên tắc chốt với user: **2K nhồi đại trà/mảng-lớn-tiling, 4K chỉ điểm-nhấn-cận-cảnh** (mảng lớn nét hơn = hạ tileSizeMeters chứ không lên 4K). |
| 2026-06-07 | **Ground khoét = polygon-boolean (Martinez) + nested-tabs G-level + cut highlight xám-khi-chọn.** **Khoét THẬT (`fromState.ts` `layerGeometry` viết lại):** thay clip-hole earcut bằng **`polygon-clipping` difference** (`zone − cut+nước`) → MultiPolygon (cut chẻ zone nhiều mảnh + lỗ) → `ExtrudeGeometry(Shape[])`; rỗng (phủ trọn) → **`null` → bỏ mesh** (xóa sạch). Gỡ ~90 dòng Sutherland-Hodgman/pointInPoly/coversXZ. Sửa 3 bug (→ **KI-011**): (a) earcut-hole chạm biên tròn/ellipse → **pie-slice wedge**; (b) cover-test ray-cast nhập nhằng ở biên → cut==zone (cùng 3000mm) **không xóa hết**; (c) gotcha `polygon-clipping` ESM **chỉ export default** (`.d.ts` named lệch runtime → `{difference}`=undefined) → import default + verify bằng **esbuild bundle** (9 case carve PASS). **UI (`gui/site.ts`):** nested-tabs `G0|G1|G2…|＋`, mỗi G = `[Mảng add|Khoét cut]` → instance `Z*|C*|＋`; **chỉ tab Khoét cut tô xám** (G/Mảng add giữ nâu, scoped `.ap-cut-tabbtn`/`.ap-cut-pane`); state `GroundLayer` +`level`. **Cut highlight (`fromState` `buildLevelCutPatches` + Lab `_applyCutVisibility`):** mảng xám **ẩn mặc định**, hiện CHỈ khi layer active (chọn tab GUI `onChange`/click 3D/Move-drag) qua `ctx.setActiveGroundLayer`; raycaster vẫn pick dù ẩn. +`polygon-clipping@0.15.7` (0 dep con, ~15KB gz, chỉ chạy lúc rebuild). Tài liệu: `playbooks/ground.md` (recipe + Y-stacking) + KI-011. Gate site-kit eslint (chỉ fence cố hữu) + archplan tsc/eslint + check-perf + check-playbooks 0-error xanh. **Đã verify :3002.** Defer: thêm G rỗng (model đếm level tường minh) + free-bezier editor ground. |
| 2026-06-06 | **Ground free-form (patch + cut-reveal, parametric) + "bộ nền" phím tắt (Phase 2 increment 1).** site-kit `GroundLayer` +`shape`(rect/circle/ellipse — reuse `shapeToLocalPolygon`) / `points` / `op`(add\|cut). **`layerGeometry` viết lại:** contour theo shape (thay rect hard-code) + ĐỤC LỖ nước/cut clip TỔNG QUÁT — `clipToConvexPolygon` (Sutherland-Hodgman tổng quát `clipToRect` cho contour LỒI rect/circle/ellipse) / free concave = fully-inside (`pointInPolyXY`). **`buildGroundLayers` tách add/cut:** layer `op:'cut'` KHÔNG dựng mesh — polygon vào `holes` → khoét MỌI add-layer LỘ base ("form trống cắt ground"). Editor GUI ground-layer (`appendLayerShapeMode`) +Form(rect/tròn/ellipse)+Mode(add/cut). **"Bộ nền":** 4 nút surface drawer trái → **BỎ hiển thị hoàn toàn**, đổi nền bằng **PHÍM SỐ 1..N** (none/stone/asphalt/sand; texture nền MỚI push CUỐI `opts` = phím kế); keydown window (bỏ qua input/ctrl/meta/alt), tự gỡ khi panel rời DOM. Gate site-kit eslint (chỉ fence cố hữu) + archplan tsc/eslint + check-perf 0-error xanh. **Đã verify :3002.** Kế (đã chốt với user): tái cấu trúc **`groundLevels[]`** (zones+cuts mỗi level, **cut lộ lớp NGAY DƯỚI** G2→G1) + nested tabs tông Ground + free-bezier editor ground. |
| 2026-06-06 | **Hồ FORM TỰ DO — circle/ellipse/bezier 2-tay-cầm + coping bo-cong (Phase 1 free-form).** site-kit **`shapes.ts` (MỚI)**: `shapeToLocalPolygon` tessellate rect/circle/ellipse (`EllipseCurve`) / free-**bezier** (`CubicBezierCurve`, 2 tay-cầm in/out ĐỘC LẬP mỗi đỉnh) → polygon LOCAL mét; `offsetPolygon` (miter pháp-tuyến-đỉnh, tự dò winding) inflate vành. `WaterConfig.shape` +`circle`/`ellipse` (TÁI DÙNG width/depth = đường-kính/2-trục); `WaterPoint` +`inX/inZ/outX/outZ` (parse tolerant, **KHÔNG bump schema**). **`pondWorldXZ` viết lại** = tessellate+offset → **SINGLE SOURCE**: basin floor/walls, lỗ-nền (`lotShape`), carve layer, **cột-đâm-đáy** (`postDropAt`), cỏ-né TỰ THEO đường cong. **`buildPoolEdge`+`waterCarveWithEdge` bo-cong** (`offsetPolygon` thay khung bbox → coping ÔM hình hồ). Editor: GUI Form +**Tròn/Ellipse/Free(bezier)**; seed free = **8 điểm** chu-vi + tay-cầm Catmull-Rom mirror (→ blob mượt); `waterDrag` +session `'handle'` + tay-cầm in/out (sphere cyan + LineSegments nối) **drag live** (`setShape` tessellate). Trade-off: tessellate dư (render-time, memoize defer) + earcut KHÔNG guard self-intersection + offsetPolygon concave-gắt tự-cắt — đều minor/edge-case; **production viewer/demo KHÔNG ảnh hưởng** (editor tree-shake, tessellate build-time 1 lần, per-frame=reflector shape-độc-lập). Gate site-kit eslint + archplan tsc/eslint + check-perf 0-error xanh. **Đã verify :3002** (3 form + coping cong + cột-đâm-đáy cong). Phase 2 (ground patch + cut-reveal, reuse cơ chế carve) kế. |
| 2026-06-06 | **wood-deck tách deck↔cột + cột-đâm-đáy-hồ (xuyên domain) + LOD móng tách khỏi LOD tường.** **wood-deck (Structure.ts):** deck và cột chống = **2 mesh + 2 material RIÊNG** (deck=`woodMaterial` Wood-planks; cột=`understructMat` Old-plywood/Tree-bark — KHÔNG chung texture); cột **lùi mép deck** `deckPostInset` (default 50mm) + **tiết diện chỉnh được** `deckPostSize` (default 120mm). GUI wood-deck (`buildWoodDeckControls`) thêm **Post mat / Post inset / Post size**. **🌊 Cột-đâm-đáy-hồ (building-kit ↔ site-kit, giữ domain boundary):** type domain-neutral **`GroundDrop`** (polygon world + `dropY`) bơm qua `BuildRenderCtx`; editor `_groundDropsForBuild` lấy từ `renderWaters` (pool+pond) → `pondWorldXZ` + `depthY/1000+2cm`. `postDropAt` (point-in-polygon, xoay rotY) per-cột → cột nằm trên hồ kéo dài tới đáy basin (wood-deck=lưới cột; stone-pillar=**trụ đá giữa**, khung-dưới lơ-lửng không đụng). Geometry **0 tri thêm** (box/cylinder cao hơn). Hạn chế đã verify: `_applySite` KHÔNG rebuild building → đổi sâu/dời hồ SAU thì cột stale tới khi building rebuild (bật/tắt Move/▶Build/reload refresh). **LOD móng tách (`plainFoundation` ≠ `plainWalls`):** Move-mode xem-tĩnh + kéo-nguyên-nhà (translate, 0 rebuild) GIỮ cột hiện (định vị nhà trên hồ); chỉ element-drag (rebuild/frame, cờ `_dragShowFound`) mới ép concrete. **Refactor:** tách `buildFancyRails` khỏi `makePositionedBalcony` (Rule-50, behavior giữ nguyên). Gate building-kit eslint + archplan tsc/eslint + check-perf 0-error xanh. **Chưa run pixel — chờ test :3002** |
| 2026-06-06 | **Bầu trời ngày↔đêm + viewer/demo nhẹ + sun radius ×2 + sun-toggle hết-lag + rippled_sand nền-editor + preset stone-pillar.** **Module `components/SkyGradient`** (WebGPU `scene.backgroundNode`, KHÔNG mesh → né lỗi "thấy quả cầu từ ngoài"): gradient zenith→horizon lerp **ngày↔đêm theo độ-cao sun** + quầng nắng/hoàng hôn; `setSun(dir)`→day-factor mờ `hemiLight`/`environmentIntensity` lúc tối; tích hợp editor qua `_applySunToSky` (live uniform). **Sun radius 24→48** (`_applySun r` + `SunGizmo DOME_R` + shadow `far` 60→90 kẻo clip) — sun không sát đất. **Sun-toggle hết lag 1–3s:** tắt = `intensity 0` (giữ `visible=true`) thay vì đổi `visible` (đổi tập-đèn-active → WebGPU recompile MỌI NodeMaterial); INP `ap-sun-toggle` ~1280ms→vài chục ms. **🏖️ rippled_sand** thêm vào surface-row drawer trái (cạnh none/stone/asphalt) — photo PhotoGround world-XZ (`_ensureEditorSandTex` async + fallback màu cát). **2 entry Vite nhẹ deploy:** `viewer.html` (portfolio orbit-ngắm, `BuildingRenderer` headless) + `demo.html` (tool-demo: nhà + cỏ + **kéo slider giờ** → nắng/bóng/vệt-cỏ live) — bundle riêng tree-shake bỏ editor. **Preset stone-pillar mặc định** (`mkStructure`): foundType=stone-pillar, foundH 4000, Expand 1m, deck Wood-planks + lan-can (4800×5100×700), khung-dưới 4500 Old-plywood (post 150/1000, beam 210×250, strut 16/300), trụ đá R600. Gate threejs-modules validate + archplan tsc/eslint/build + check-perf xanh. **Đã verify :3002** (sky/sun/sand/toggle). |
| 2026-06-05 | **Deploy-prep: nén texture KTX2 (dist archplan 228→81MB) + UX "câu-giờ-load" + tắt sourcemap.** Đáp "local mượt ≠ Vercel mượt": chẩn `vite build` thật → 2 nút thắt — (1) **cross-repo** (archplan import `../threejs-modules`+`../../assets` = 3 repo tách, Vercel clone đơn-lẻ build chết → chờ **Phase 2** đóng npm package `@web-3d/*`); (2) **texture 220MB** (2K nhưng encode gần-lossless, normal 9–10MB/cái). **KTX2** (toktx v4.4, cài NSIS silent không-admin): 13 set JPG→`.ktx2` per-map — basecolor ETC1S srgb / **normal UASTC RDO linear** (sàn ~5MB/2K, RDO vô-dụng-với-normal-chi-tiết → **resolution mới là đòn bẩy**; chốt giữ 2K=77MB) / rough+ao ETC1S / tất cả `--genmipmap`. `loadOne` route theo đuôi → đổi import `.jpg?url`→`.ktx2?url` (0 sửa loader) + **transcoder basis LOCAL** `public/basis/` thay CDN. THÊM .ktx2 CẠNH .jpg (assets shared — consumer khác không vỡ). **UX câu-giờ:** badge "⏳ đang tải vật liệu" (drive `onUpdate`, change-detect, CSS inline — không đụng css Factory) + **prefetch-on-open** dropdown Surface (`selectRow.onOpen`→`_prefetchGroundTextures`). `vite sourcemap:false` (prod nhẹ ~6MB + giấu source). Gotcha: `basis_transcoder.js` vendored trong `src/` → eslint `.` quét → thêm `src/public/**` vào ignores. Memory `ktx2-texture-pipeline-toktx`. Gate archplan tsc/eslint/build xanh. **Đã verify :3002.** |
| 2026-06-05 | **Click tầng ground → trỏ GUI (👆 Focus, đủ bộ 4 tương tác cho ground).** Click tầng G1+ trong 3D (mode thường) → mở drawer Ground → sub-tab Ground → instance tab **Gn** (cuộn + flash). Lab `_tryClickLayer` (raycast `siteGroup` tìm `groundLayerIdx`, nhường building nếu pick gần hơn) → `_navigateToLayer` → site `navigateToGroundLayer` → ground domain `navigateToLayer` (chọn tab `idx+1`; G0 base không tag → không focus). Set/clear `_siteNavigateLayer` ở `_buildLeftTools`/teardown. INTERACTIONS.md §5b Focus. Quy ước lệnh mới **`code1 cho <element>`** = wire Focus interaction. tsc/eslint xanh. |
| 2026-06-05 | **Móng stone-pillar 3-tab + lan can deck + GROUND ĐA-TẦNG kéo-được + 5 texture.** **Foundation `stone-pillar`** (Structure.ts) tách **3 mesh material RIÊNG**: deck gỗ ngang (+**lan can 4 mặt** dài/rộng/cao chỉnh-được, merge chung deck) · **khung-dưới** (2 tầng 8 xà + 8 trụ + 8 chống xiên — số đo `understructHalf` ĐỘC LẬP deck, texture riêng) · trụ đá giữa; GUI Foundation▸Stone pillar = **3 tab con** (Deck\|Khung dưới\|Trụ đá). Slab thêm **planks-tex** (dùng chung gỗ deck). **5 texture mới** (PROTOCOL): `Wooden_Plank`(deck+slab)/`Old-piwood`+`tree_bark`(khung-dưới)/`worn_pavement`+`roman_stone_floor`(ground). **site-kit GROUND ĐA-TẦNG**: `SiteState.groundLayers[]` (additive, KHÔNG bump schema) — mỗi tầng = **box ĐỘC LẬP** dài×rộng×dày (max 40m) + offset, **xếp chồng Y**, **tự đục lỗ né pool/pond/puddle + dải edge** (`layerGeometry` clip Sutherland-Hodgman → ExtrudeGeometry hole); GUI Ground = instance-tab **G0(base)\|G1\|G2…+＋**; **G1+ kéo-được Move** (perf: dời `mesh.position`, 0 rebuild → re-carve khi thả; G0 cố định). **Cache PhotoGround per-key** (Lab-owned `_groundMat`, inject `groundMatByKey` → nhiều ground cùng key dùng CHUNG material, hết recompile mỗi rebuild). Deferred: `ground-layer-carve-reveal` (khoét lộ lớp dưới). Gate lõi(building+site)+archplan tsc/eslint + check-perf xanh. **Chưa run pixel — chờ test :3002** |
| 2026-06-04 | **Vá lỗ-hổng dispose RTT (reflector + PMREM) — audit hệ-thống.** Audit toàn repo dispose: cấu trúc SẠCH (mọi component/shader có dispose; texture brick/material/shader cache/shadow đều free). **Lỗ hổng RTT âm thầm DUY NHẤT = `reflector()` (chỉ `WaterSurface` dùng)**: three giữ RTT trong `WeakMap` + không expose dispose → `material.dispose()` không đụng → leak GPU. **Fix tại nguồn:** `WaterSurface.setCamera()` + `dispose()` truy chuỗi `viewCam→virtualCameras→renderTargets→RT.dispose()` (cast `ReflectorBaseLike`, optional-chain phòng three đổi field); editor gọi `setCamera` trong `_rebuildSite`. **PMREM env:** giữ ref `RenderTarget` → dispose cả wrapper (không chỉ `.texture`). ⇒ code hiện tại leak-free (mỗi `_clearSite` dispose nước free đúng RTT). `KI-006` + WaterSurface README. Đánh đổi: truy field nội-bộ three (`scan-versions.js` soi drift). Gate tsc/eslint xanh. **Verify cần DevHud: textures KHÔNG leo khi sửa site.** |
| 2026-06-04 | **Perf "sửa-1-thứ-rebuild-tất" + puddle render + định-vị-hồ + foundation khít + UX Move.** **Perf (KI-005):** 3 nguyên-nhân-riêng, 3 fix — (1) cỏ re-scatter mỗi edit → **dirty-check `_grassSig`** (cỏ ở group bền, lõi thêm `skipGrass`+`buildSiteGrass`/`grassBuildSig`/`siteGrassExclude`, hoãn lúc kéo); (2) reflector RTT churn kéo nhà → **dirty-check `_siteSig`** (`{...site,grass3d:0}`); (3) **brick-3D instancing** dựng-lại 60×/s = tụt 60→20fps → **LOD** `renderBuildingState(…, plainWalls)` bỏ instancing lúc kéo. Đã thử-rồi-GỠ (đánh nghi-can-sai): skip-shadow/pick, transform-fast-path, "bóng ma" `transparent` (→ transparent-pass cũng tụt fps). **Puddle render**: `renderPuddles`+`buildPuddle` mặt nước PHẲNG trên nền (no đáy/coping/lỗ); GUI Pe = Shape\|Surface. **Định-vị-hồ khi kéo**: mặt nước chui dưới đất (lỗ chưa theo) → **mảng cyan mờ** (`_showWaterOutline`, ShapeGeometry vẽ-trên-cùng) ở mặt nền theo cả 3D-drag lẫn slider (`previewWater`, rẻ, không reflector→không leak) cho **pool/pond/puddle**. **Foundation khít**: `foundOh` default 0.3→**0** (móng = footprint ngoài 4 tường, ghép khối liền mép). **UX:** bật Move(Z)→cả nhà nhuốm xanh "nghệ" ngay (`_setBuildingTint` theo moveMode); **click thường** vào mặt nước→trỏ GUI tab hồ; **phím X** thả menu palette. Gate lõi+vỏ tsc/eslint xanh. **Chưa run pixel — chờ test :3002** |
| 2026-06-04 | **Hồ ĐA-INSTANCE + lồng tab 5 bậc + coping + chất liệu + fix mép xanh.** Lõi site-kit: `WaterConfig` thêm `kind`(pool/pond/puddle)+`edgeWidth`(coping 500mm)+`floor/wall/edgeMaterial`; `SiteState.water`→**`waters[]`** (migrate tolerant, KHÔNG bump schema); **`renderWaters`** render pool+pond (puddle placeholder; pond "y như" pool, param sau); `buildWater` loop nhiều hồ (mỗi `reflector` = +1 RTT → instance mới `enabled=false` né tụt FPS); **`buildPoolEdge`** dải coping rect-frame quanh hồ; **fix "đường xanh cỏ" ở mép hồ**: nền lô khi có hồ dựng **PHẲNG** (`ShapeGeometry`@rim, bỏ `ExtrudeGeometry` dày → hết mặt-cắt slab) + vách basin **`yTop=rim`** (phủ rim→đáy) ⇒ coping↔tường liền. Vỏ archplan: tab **Water tông XANH curated** (`--wt-*`) **LỒNG 5 bậc** — Pool\|Pond\|Puddle ▸ Pl/Pd/Pe instance **+＋/✕** (đa-hồ, active=tab đang chọn → 3D drag/handle/tune nhắm đúng) ▸ **Pool edge\|Surface\|Bottom** ▸ **Floor\|Walls** + select **Material** placeholder ('none'); slider water **track trắng** (trước bg-3 trùng nền→tàng hình); tab instance cố-định-nhỏ; drag/carve **multi-pool** (raycast mọi mesh hồ, khoét nhiều lỗ + `keepSpans` cắt lưới). **Hàng Build/Reset/Save/Load/JSON + undo/redo → footer DÙNG CHUNG đáy drawer** (ra khỏi panel Building, bền qua `_rebuildGUI`). Gate lõi+vỏ tsc/eslint + check-playbooks xanh. **Chưa run pixel — chờ test :3002** |
| 2026-06-04 | **Fix hồ NHÌN-THẤY-ĐÁY (vá 3 bug che đáy) + Move ra float/phím Z.** Hồ bật mà không thấy đáy/lòng — 3 bug chồng: (1) `buildBasin` `mergeGeometries` trộn indexed/non-indexed → trả null (mất đáy lặng lẽ) ⇒ `floor.toNonIndexed()` trước merge; (2) 2 lớp nền đặc che basin dưới y=0 ⇒ vỏ `_rebuildEditorGround` (backdrop = `ShapeGeometry` khoét lỗ) + `_buildGridGeo` (lưới = `LineSegments` tự dựng cắt bbox hồ; `GridHelper` không hole được); (3) `baseY` chìm kẹp theo ĐÁY (slab mỏng ~1cm, không theo slab). `pondWorldXZ` export = single-source polygon lỗ (lõi+vỏ dùng chung). Gương hết "đứng hình" lúc orbit thấp: `reflector.forceUpdate`/frame + `frustumCulled=false` (guard `isFacingAway`). Vỏ archplan: nút **Move → float góc-trái-dưới** (ra khỏi drawer) + **phím Z** (Alt cũ đụng OS). Catalog `KI-004`. Gate kit validate + archplan tsc/eslint xanh. **Chưa run pixel — chờ test :3002** |
| 2026-06-04 | **site-kit G1a cỏ đổ-bóng + G3 hồ nước (tier C) reflect+refract.** **Cỏ:** `GrassBlades` thêm **vệt-tiếp-đất giả theo MẶT TRỜI** (InstancedMesh quad con, hướng/dài/đậm = uniform `setSun`, đậm theo góc-cao-sun; fake AO thay shadow-map sub-texel — KI-003 `positionLocal.add` né mất instanceMatrix) + **2 mặt 2 MÀU RIÊNG** (`uInnerColor` thay ×0.5 cứng) + nhận-bóng. **Hồ `WaterSurface` (components/, tier C):** `reflector` gương + **`viewportSharedTexture` khúc xạ NHÌN XUYÊN ĐÁY** Fresnel-blend + đốm nắng `setSun` + **form tự do** (polygon `ShapeGeometry`, kéo đỉnh) + **basin** (vách+sàn) + **khoét lỗ nền** (`ExtrudeGeometry`+`Shape.holes`); `WaterConfig` (shape/points/offset/depthY/bottomColor/tint…) + `parseSite`. Vỏ archplan: sub-tab 💧 Water + **kéo hồ/đỉnh trong 3D** (Move tool, **phím Alt** toggle) + 🎛️**Lab** (đổi tên, reorder Số đo, Bóng đổ tách màu-2-mặt/vệt-toggle). Deferred: `water-bottom-refraction` (C PBR transmission + vision lòng-hồ-tương-tác + physics G/IOR). Gate kit validate + archplan tsc/eslint xanh. **Chưa run pixel — chờ test :3002** |
| 2026-06-03 | **GrassBlades chi tiết — lá/bụi cỏ tả thực + 2 mặt + garden-patches deferred.** Lõi `components/GrassBlades` (tier B): rộng GỐC/THÂN riêng + thon ngọn (mép ellipse) + **cong T→P** + **cong DỌC 1 chiều** (mặt ngoài +Z lồi) + **CỤP 1 chiều** (mặt trong −Z lõm) **2 lựa chọn** shader-normal (rẻ, zero tris) / geometry-fold (`cupGeo`, ×3 tris, ẩn) + **2 MẶT 2 MÀU** (`frontFacing` → ngoài đầy / trong ×0.5) + **bụi cỏ** (`bladesPerClump` gộp K lá: mặt trong quay vào tâm, `clumpSplay` xòe chống đâm xuyên, rải cụm=mật độ÷K → **budget-neutral**). Vỏ archplan: panel 🎛️ Tinh chỉnh **tách tab Lá đơn\|Bụi cỏ** (Tabs xanh rêu) + ô số mm/cm + **preview OrbitControls** (xoay/pan/zoom) + nền gradient studio. Deferred mới: `garden-ground-patches` (mảng bê tông/cát/**nước** exclude cỏ — cắt ~1000 tris/m²), `voronoi-applications`, `procedural-generation-techniques`. Gió (B6) = bước sau; va chạm lá-lá = không khả thi (instanced). Gate kit validate + archplan tsc/eslint xanh |
| 2026-06-02 | **site-kit G0 — sân vườn / lô (nền + rào).** Domain lõi mới `threejs-modules/site/` (anh em building/, ĐỘC LẬP nó): `site/state` (`SiteState` nền+rào + `GROUND_PRESETS` + `coverageStats` 建ぺい率 + `parseSite`) + `site/render/fromState` (`renderSiteState` headless — nền slab dày 1–10cm đáy y=0 hết z-fight grid; rào gỗ cọc+thanh / tường liền, merged 1 mesh). Vỏ archplan: panel 🌳 Sân vườn (`gui/site.ts`) + **bảng số liệu live** đối chiếu nhà/lô (phủ%, cảnh báo >60%); `_renderSite` đôn building+pick lên `groundThick` khi `show` (foundation nằm trên mặt nền); DesignStore **compose `site`** additive — building v10 **bất biến**, file cũ vẫn mở. Khung: 建ぺい率 nhà ở Nhật 30–60%, mặc định lô ~96 m²/50%. Gate kit+archplan tsc/eslint/build + Doraemon tsc xanh. Tracker `PLAN-lot-site-garden.md`; ADR-005 (lõi). Phase G1 cây / G2 đá / G3 hồ cá deferred |
| 2026-06-01 | **Thin-out archplan → LÕI (Phase 0–4) + retire AP4.** Đẩy logic dựng-nhà xuống building-kit: `state.ts` BuildingState schema (P0+shim); `build.ts`+renderer → `render/fromState` (`renderBuildingState` + `class BuildingRenderer` headless; ArchPlanLab delegate, pick-box ở vỏ qua `Placement[]`; 1396→~950 dòng, P1). **Retire `BuildingFromPlan`** (AP4 lossy, dormant 0 caller) → **Gap 2 đóng bằng construction** — 1 renderer canonical đọc `BuildingState` lossless (stairs/balcony/roof-rot/paint full fidelity). Orphan dọn: `parts/Stair.ts`, lớp AP4 export (`buildingStateToJSON`+3); nút 📄 JSON → `serializeDesign` (lossless, Load lại được). Reorg `building/` → `render/`+`preset/`, engine primitive flat, bỏ prefix "Building" ở filename (giữ tên export/class). tsc/eslint/build 3 project xanh. Tracker `PLAN-thin-out-archplan.md`. Thuật ngữ: "lõi" (không "hạt nhân") |
| 2026-06-01 | **Gap 1 đóng — material fidelity headless.** Tách wall-pipeline ra building-kit dùng chung: `wallMaterials.ts` (WallMaterialCache + surface shaders + brick-tex, +textures/bricks/) + `wallAssembly.ts` (`assembleWall` dispatch 5 nhánh material + `mergeWalls`). Editor (archplan) + headless (`BuildingFromPlan`) CÙNG gọi → nhà build từ ngoài render đúng shader-surface/instanced như editor (hết toon phẳng), chống drift KI-001. AP4 export chở đủ field material. tsc/eslint/build 3 project xanh. Gap 2 (full BuildingState, bỏ AP4 lossy) còn deferred |
| 2026-06-01 | archplan rã monolith tiếp — `ArchPlanLab.ts` **1950→1529** (−421, −22%): tách `gui/devhud.ts` (DevHud), `interaction/highlight.ts` (HighlightOverlay, host-pattern), `state/persistence.ts` (DesignStore I/O) + `footprintXZ` về `build/build.ts`. README archplan ghi taxonomy + 2 pattern tách (host-interface / pure-service). Build pipeline ~650 dòng để tách riêng (unify với headless). tsc/eslint/vite build xanh |
| 2026-06-01 | Catalog audit + sync README — thêm 6 module bị drift (BaseShaderMaterial, AsphaltGround, BaseGPUEffect, BeamEffect, BillboardSprite, ShockwaveRing) + cột **Status** (in-use/base/idle) theo grep consumer thật. Lõi proven = building-kit + BaseWorld + surface/wall archplan; phần lớn còn lại 🗄 idle (built-ahead) |
| 2026-06-01 | building-kit hardening headless — `turtle.ts` core dùng chung (editor `build.ts` + headless `BuildingFromPlan` hết chép tay turtle-walk → chống drift KI-001); `BuildingFromPlan` đọc **wallH per-segment** (trước ép `floorH=max` san phẳng). Material-fidelity (toon→shader) defer |
| 2026-05-30 | Category `ui/` mới (widget DOM thuần) — `Tabs` folder-style: tablist + ARIA + roving tabindex + keyboard, theme CSS vars; tách từ ArchPlanLab. validate.js whitelist thêm `ui` |
| 2026-05-30 | Fix `WoodSidingStrip` + `InstancedBrickWall` solidTraps boundary `!b\|\|!t` (cùng bug copy-paste, propagate 7b171a6) — hết răng cưa mép lỗ cửa/sổ. Chi tiết: 01-Doraemon `known-issues/KI-001` |
| 2026-05-30 | `components/` — wall geometry THẬT: `InstancedBrickWall` (gạch InstancedMesh + khoét lỗ), `WoodSidingWall` (clapboard), `WoodSidingStrip` (ribbon 1 khối mergeable + openings), `AsphaltGround` |
| 2026-05-23 | Phase G hoàn thành — AudioSystem (spatial PositionalAudio, load/cache/play, tích hợp CollisionEventBus.onImpact) |
| 2026-05-19 | Phase F mở rộng — CollisionEventBus (event bus collision → handler, force threshold, ImpactEvent dispatch cho VAT/Particle/Audio) |
| 2026-05-18 | Phase F hoàn thành — PhysicsWorld (Rapier WASM wrap), RigidBody (dynamic/fixed/kinematic + cuboid/ball/capsule), CharacterController (collision-resolved movement + jump + gravity) |
| 2026-05-18 | Phase E hoàn thành — InteractionSystem (Raycaster hover/click), AnimationSystem (AnimationMixer crossfade), ScrollTimeline (CatmullRomCurve3 scroll-driven camera) |
| 2026-05-15 | Phase D hoàn thành — PostProcessing (bloom WebGPU), WindAnimation (triNoise3D positionNode), DayNightCycle (sun arc + ambient lighting) |
| 2026-05-15 | Gallery update — thêm 6 modules Phase C + D: VATShader, LODBillboard, CharacterPool, PostProcessing, WindAnimation, DayNightCycle |
| Ngày       | Thay đổi                                                                   |
| ---------- | -------------------------------------------------------------------------- |
| 2026-05-14 | Module Gallery hoàn thành — gallery.html với 10 live Three.js canvas cards, lazy-load qua IntersectionObserver, refactor toàn bộ example.ts sang createDemo(canvas) pattern |
| 2026-05-15 | Phase C hoàn thành — CharacterPool unit-pass: generic pool<T>, acquire/release O(1), warnThreshold analog RuntimeGuard |
| 2026-05-15 | LODBillboard unit-pass: THREE.Sprite + SpriteMaterial, WebGPU auto-upgrade, LOD.addLevel với Object3D, getCurrentLevel() |
| 2026-05-15 | Phase C bắt đầu — VATShader unit-pass: positionNode + normalNode từ DataTexture, vertexIndex TSL, update(time) loop |
| 2026-05-14 | Phase B mở rộng — thêm `GPUParticleSystem` (base class), refactor `SparkSystem` thành preset (composition). Phase B: 5/5 unit-pass |
| 2026-05-13 | Phase B hoàn thành — 4/4 modules unit-pass (LODSystem, ProceduralFracture, InteriorMapping, SparkSystem) |
| 2026-05-13 | Phase A hoàn thành — 5/5 modules unit-pass |
| 2026-05-12 | Tạo file — tổng hợp từ `00-Threejs/ROADMAP.md` + `CLAUDE.md` Phase A build order |
